# Material Depot — Project Context for Claude

## Project Overview
Role-based web app for Material Depot's field operations. Plain HTML/CSS/JS, no framework, no build step.
- **Live URL**: https://material-depot-site.vercel.app
- **GitHub**: https://github.com/daaku-daddy/material-depot-site (branch: `master`)
- **Vercel project**: `material-depot1/material-depot-site`
- **Local clone**: `/Users/dhruv/Projects/material-depot-site/`
- **Deploy command**: `vercel --prod` from the local clone directory

## Files
| File | Role |
|---|---|
| `index.html` | Meta-refresh redirect to `Login.html` |
| `Login.html` | Passcode login, writes `md_user` to **localStorage** |
| `Admin.html` | Admin console — overview, users, role viewer, job overview (with job cards), performance, analytics. Also accepts `service_mgr` sessions, scoped to Analytics only (note 73) |
| `SM_Audit_Dashboard.html` | Service Manager — site audit order lifecycle |
| `SM_Install_Dashboard.html` | Service Manager — installation order lifecycle |
| `Site_Auditor_App.html` | Field auditor mobile app (max-width 520px) |
| `Site_Installer_App.html` | Field installer mobile app (max-width 480px) |
| `vercel.json` | Cache-Control: no-cache for all HTML files |
| `docs/supabase_slim_views.sql` | SQL to create `install_orders_slim` view (strips photos from subjobs for bandwidth) |
| `pwa-install.js` | Shared PWA install banner — included in all 8 HTML files (Login, Admin, both SM dashboards, both field apps, Store Team, Site Shadower) |
| `manifest_auditor.json` | PWA manifest for Site_Auditor_App (`start_url: /Site_Auditor_App.html`) |
| `manifest_installer.json` | PWA manifest for Site_Installer_App (`start_url: /Site_Installer_App.html`) |
| `manifest_sm_audit.json` | PWA manifest for SM_Audit_Dashboard (`start_url: /SM_Audit_Dashboard.html`) |
| `manifest_sm_install.json` | PWA manifest for SM_Install_Dashboard (`start_url: /SM_Install_Dashboard.html`) |
| `manifest_admin.json` | PWA manifest for Admin (`start_url: /Admin.html`) |
| `Store_Team_App.html` | Store team slot booking app for experience centre walk-ins (max-width 520px) |
| `manifest_store_team.json` | PWA manifest for Store_Team_App (`start_url: /Store_Team_App.html`) |
| `Site_Shadower_App.html` | Field trainee "shadow" app (max-width 520px) — read-only daily schedule of audits they are assigned to observe, no job card yet (see note 85) |
| `manifest_shadower.json` | PWA manifest for Site_Shadower_App (`start_url: /Site_Shadower_App.html`) |

## Stack
- Plain HTML/CSS/JS — no framework, no build step
- Supabase REST API (raw fetch, no JS client library)
- Vercel static hosting
- jsPDF + jspdf-autotable for PDF generation (CDN, loaded in all files)

## Supabase
- **URL**: `https://jqrdfnjfxqxrazfkaofm.supabase.co`
- **Anon key**: `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImpxcmRmbmpmeHF4cmF6Zmthb2ZtIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODEwOTU5NTcsImV4cCI6MjA5NjY3MTk1N30.2mvCPc0E_vDn2WaID5sEjwU4Dyj53rhevGrSPBa3__g`
- All pages use the anon key with REST API
- Standard helpers present in every file: `sbGet(query)`, `sbPost(table, body)`, `sbPatch(table, id, body)`, `sbDel(table, id)`
- No RLS — anon key has full read+write access

### Table: `profiles`
- Columns: `id` (uuid), `name`, `email`, `role`, `passcode`, `installer_type`, `created_at`, `active_from` (text, nullable), `training_stages` (jsonb, default `[]`)
- Roles: `admin`, `service_mgr`, `site_auditor`, `installer`, `auditor_installer`, `store_staff`, `site_shadower`
- `training_stages`: ordered jsonb array of `{role, start, end}` (dates inclusive ISO `YYYY-MM-DD`, `role ∈ {'site_shadower','site_auditor'}`) — a SM trainee's dated **secondary** stages. `role` stays `service_mgr` (primary); the secondary role active today drives a toggle to that dashboard. Set from Admin → Users → Edit Role → Training schedule. See note 85. `site_shadower` is only ever a secondary/stage role, never a primary `role` value in the current model — so `profiles_role_check` does NOT need it (the constraint block below leaves it out on purpose).
- `installer_type`: `'flooring'` or `'wallpaper'` (required for installer + auditor_installer)
- `passcode`: 4-digit string, null until first login
- `active_from`: ISO date string (`YYYY-MM-DD`) for site_auditor/auditor_installer — null = active always; a date = not available before that date. Set from SM Audit Dashboard → Auditors & Caps → Save. Read by Store_Team_App to count active auditors per date.
- **DB constraint**: `profiles_role_check` — update via SQL Editor when adding roles

**Pending SQL** (must be run in Supabase SQL Editor if not yet done):
```sql
-- Allow store_staff role:
ALTER TABLE profiles DROP CONSTRAINT IF EXISTS profiles_role_check;
ALTER TABLE profiles ADD CONSTRAINT profiles_role_check
  CHECK (role IN ('admin','service_mgr','site_auditor','installer','auditor_installer','store_staff'));

-- Add active_from column for auditor availability management:
ALTER TABLE profiles ADD COLUMN IF NOT EXISTS active_from text;

-- Training lifecycle (note 85) — dated secondary stages for SM trainees:
ALTER TABLE profiles ADD COLUMN IF NOT EXISTS training_stages jsonb DEFAULT '[]'::jsonb;
-- (profiles_role_check intentionally NOT extended for site_shadower — it's never a primary role.)

-- City segregation (note 88) — Bengaluru/Hyderabad on profiles + both order tables:
ALTER TABLE profiles       ADD COLUMN IF NOT EXISTS city text;
ALTER TABLE audit_orders   ADD COLUMN IF NOT EXISTS city text;
ALTER TABLE install_orders ADD COLUMN IF NOT EXISTS city text;
UPDATE profiles       SET city='Bengaluru' WHERE city IS NULL;
UPDATE audit_orders   SET city='Bengaluru' WHERE city IS NULL;
UPDATE install_orders SET city='Bengaluru' WHERE city IS NULL;
-- THEN re-run docs/supabase_slim_views.sql (install_orders_slim now selects `city`).
-- Until ALL of the above run, the city feature stays inert (probe-gated) — see note 88.
```

### Table: `audit_orders`
- Columns: `id`, `created_at`, `pi`, `po` (text, comma-joined), `skus` (jsonb), `audit_ticked` (jsonb), `audit_ticked_history` (jsonb array — see note 66), `bm`, `customer_name`, `phone`, `addr`, `status`, `service` (jsonb), `slot`, `date`, `auditor_id`, `auditor_name`, `auditor_email`, `shadower_email` (text, nullable), `shadower_name` (text, nullable), `log` (jsonb array), `created_by_email`
- `shadower_email`/`shadower_name` (note 85): optional trainee assigned to *observe* this audit — set alongside the auditor in the SM Audit drawer, read by `Site_Shadower_App.html`. Nulled whenever the order is moved back before `assigned`. SQL: `ALTER TABLE audit_orders ADD COLUMN IF NOT EXISTS shadower_email text; ALTER TABLE audit_orders ADD COLUMN IF NOT EXISTS shadower_name text;`
- Status flow: `pending → created → scheduled → assigned → onway → atsite → completed / reschedule`
- Special statuses: `deleted` (soft delete), `call_na`, `slot_reserved` (pre-booking via Store Team app — no PI required at time of booking)
- `audit_ticked` shape (new format): `{auditor, date, sign: {img, name, ratings: {q1, q2, q3, comments}}, rooms: [{name, type, sku, calc, notes, photos: [], sketchStrokes: []}]}`
- `audit_ticked` old format (legacy): array of strings like `["Wooden Flooring"]`
- `audit_ticked_history`: array of past-signed `audit_ticked` snapshots, each with an added `archivedAt` (ISO timestamp) and `archivedReason` (`'completed'` | `'backfill-on-reopen'` | `'status-changed-to-<status>'`) — permanent record of every completion, independent of whatever the live `audit_ticked` currently holds. See note 66.

**Pending SQL** (must be run in Supabase SQL Editor if not yet done — added 2026-07-25, see note 66):
```sql
ALTER TABLE audit_orders ADD COLUMN IF NOT EXISTS audit_ticked_history jsonb DEFAULT '[]'::jsonb;
```
- `service` shape: `{flooring: [{sku, name, link}], wallpaper: [{sku, name, link}], audit_by: 'material_depot'|'customer', follow_up_date?: "YYYY-MM-DD"}`
- `service` extended for rectification: adds `rectification_raised: true`, `rectification_pi`, `rectification_type`
- **SM displays `assigned` as "Site Auditor Assigned"; auditor app maps DB `assigned` → local `scheduled`**
- **⚠️ POLL NOTE**: SM_Audit_Dashboard polls `audit_orders` with explicit column select (NO `audit_ticked`) to avoid downloading photos. `audit_ticked` is fetched on-demand for PDF and job card screen.

### Table: `install_orders`
- Columns: `id`, `created_at`, `pi`, `po`, `skus` (jsonb), `bm`, `customer_name`, `phone`, `addr`, `matched_audit` (bool), `delivery_date`, `original_delivery_date` (text — set at creation, never overwritten on delays), `custom_wp` (bool), `custom_wp_stage` (text — see note 62), `custom_wp_meta` (jsonb — see note 62), `status`, `subjobs` (jsonb array), `service` (jsonb), `log` (jsonb), `created_by_email`

**Pending SQL** (must be run in Supabase SQL Editor if not yet done — added 2026-07-23, see note 62):
```sql
ALTER TABLE install_orders ADD COLUMN IF NOT EXISTS custom_wp_stage text DEFAULT 'draft';
ALTER TABLE install_orders ADD COLUMN IF NOT EXISTS custom_wp_meta jsonb DEFAULT '{}'::jsonb;
```
Also re-run `docs/supabase_slim_views.sql` (`install_orders_slim` view was extended to include these two new columns).
- Status flow: `pending → deliv_ontime / deliv_delayed → created → scheduled → assigned → onway → atsite → completed / partial / reschedule`
- Special status: `deleted` (soft delete)
- `subjobs` shape (current multi-installer format):
  ```json
  [{
    "id": "sj_fl",
    "type": "flooring",
    "assignments": [{
      "installer_id": "uuid", "installer_email": "email", "installer_name": "Name",
      "mode": "standard|custom", "date": "YYYY-MM-DD", "slots": ["HH:MM"],
      "dates": ["YYYY-MM-DD"], "status": "assigned|onway|atsite|completed|reschedule", "primary": true
    }],
    "status": "created|assigned|onway|atsite|completed|reschedule|partial",
    "items": [{"sku": "WF-OAK-12MM", "name": "...", "link": "...", "qty": "12 boxes", "rolls": "5"}],
    "jobcard": {"rooms": [...], "sign": {"img": "...", "name": "...", "ratings": {"q1": 8, "q2": 9, "q3": 7, "comments": "..."}}}
  }]
  ```
- Legacy subjobs: use `sj.installer`, `sj.installer_email`, `sj.date`, `sj.slot` directly
- `primary` flag: first installer defaults to primary. `sj.status` only follows primary's status.
- `service.audit_by`: `'material_depot'` = MD did pre-audit; `'customer'` = customer-provided measurement
- **⚠️ POLL NOTE**: SM_Install_Dashboard polls `install_orders_slim` with `status=neq.deleted`. Site_Installer_App polls `install_orders_slim` with `status=not.in.(pending,deliv_ontime,deliv_delayed,deleted)`. The slim view strips `photos` from all `rooms[]` inside each `subjob.jobcard` — this dramatically reduces poll payload. On-demand fetches (PDF download, opening order detail drawer) use the full `install_orders` table directly. SQL for the slim view: `docs/supabase_slim_views.sql`.

### Table: `ratings`
- Columns: `id` (uuid), `created_at`, `order_type` ('audit'|'install'), `pi`, `order_id` (uuid), `staff_email`, `staff_name`, `q1_score` (1-10), `q2_score` (1-10), `q3_score` (1-10, added 2026-06-19), `comments`, `customer_name`, `customer_phone`
- Written after every completed job (after client signs)
- `q1` = overall experience, `q2` = staff person rating, `q3` = site cleanliness after work
- Ratings also stored in `audit_ticked.sign.ratings` / `subjobs[i].jobcard.sign.ratings`
- **DB column to add**: `ALTER TABLE ratings ADD COLUMN IF NOT EXISTS q3_score int CHECK (q3_score BETWEEN 1 AND 10);`
- Create SQL: `CREATE TABLE ratings (id uuid DEFAULT gen_random_uuid() PRIMARY KEY, created_at timestamptz DEFAULT now(), order_type text CHECK (order_type IN ('audit','install')), pi text NOT NULL, order_id uuid, staff_email text, staff_name text, q1_score int CHECK (q1_score BETWEEN 1 AND 10), q2_score int CHECK (q2_score BETWEEN 1 AND 10), q3_score int CHECK (q3_score BETWEEN 1 AND 10), comments text DEFAULT '', customer_name text, customer_phone text);`

## Auth / Session
- `localStorage` key: `md_user` → `{name, email, role, training_stages}` — persistent, no expiry (`training_stages` added note 85; absent/`[]` for everyone except SM trainees)
- Role → file routing: `admin→Admin.html`, `service_mgr→SM_Audit_Dashboard.html`, `site_auditor→Site_Auditor_App.html`, `installer→Site_Installer_App.html`, `auditor_installer→Site_Auditor_App.html`, `store_staff→Store_Team_App.html`, `site_shadower→Site_Shadower_App.html`
- SM dashboards accept both `service_mgr` and `admin` — guard: `!['service_mgr','admin'].includes(SESSION.role)`
- `auditor_installer` role: accepted by both field apps; "⇄" switch button navigates between them
- **Training toggle (note 85)**: a `service_mgr` whose `training_stages` puts them in a `site_auditor`/`site_shadower` stage today is *also* admitted to `Site_Auditor_App.html`/`Site_Shadower_App.html` respectively (guards check `activeSecondaryRole(training_stages)`), and both SM dashboards show a header "⇄" toggle to that app. A plain `service_mgr` (no active stage) is still rejected from those apps. The helper `activeSecondaryRole(stages)` is duplicated verbatim in `Login.html`(persist only)/both SM dashboards/`Site_Auditor_App.html`/`Site_Shadower_App.html`.
- **Any valid email accepted** (as of 2026-06-23): Login.html and Admin "Add New User" form no longer restrict to `@materialdepot.com`. Access is still gated by whether the email exists in `profiles`.

## Polling Intervals & Bandwidth Strategy
All polls skip when `document.hidden` (page visibility API). Resume + immediate fetch on tab visible.

| File | Interval | Query strategy |
|---|---|---|
| `SM_Audit_Dashboard` | 30s | Explicit cols, NO `audit_ticked`, `status=neq.deleted` |
| `SM_Install_Dashboard` | 60s | `install_orders_slim` view, `status=neq.deleted` (strips photos from poll) |
| `Site_Auditor_App` | 30s | Explicit cols, NO `audit_ticked`, `status=neq.deleted` |
| `Site_Installer_App` | 30s | `install_orders_slim` view, `status=not.in.(pending,deliv_ontime,deliv_delayed,deleted)` |

Deleted orders are loaded **on demand** in both SM dashboards when the "Deleted Orders" tab is clicked (`loadDeletedOrders()`). They are NOT included in the poll.

## Photo Storage (added 2026-07-01)

New photos taken in both field apps are uploaded to Supabase Storage instead of stored as base64 in JSONB columns.

### `job-photos` bucket
- Public bucket: `{SB_URL}/storage/v1/object/public/job-photos/{filename}`
- Upload: `POST /storage/v1/object/job-photos/{filename}` with `Content-Type: image/jpeg`, `x-upsert: true`
- Policies: anon INSERT + anon SELECT (no auth required)

### `_uploadPhoto(dataURL)` helper (both field apps)
```js
async function _uploadPhoto(dataURL){
  const blob=await(await fetch(dataURL)).blob();
  const fname=Date.now()+'_'+Math.random().toString(36).slice(2,8)+'.jpg';
  const r=await fetch(SB_URL+'/storage/v1/object/job-photos/'+fname,{
    method:'POST',
    headers:{'apikey':SB_KEY,'Authorization':'Bearer '+SB_KEY,'Content-Type':'image/jpeg','x-upsert':'true'},
    body:blob
  });
  if(!r.ok)throw new Error('upload failed');
  return SB_URL+'/storage/v1/object/public/job-photos/'+fname;
}
```
`_resizeAndStore` calls `_uploadPhoto(out).then(push).catch(()=>push(out))` — falls back to base64 if upload fails.
Arrival photo (`arrivalOverlay`) also uploads via `_uploadPhoto` before calling the confirm callback.

### Backward compatibility
Existing photos remain as base64 data URLs in DB — `<img src="...">` handles both data URLs and https:// URLs transparently. No migration of old photos needed.

### Supabase SQL required (one-time)
```sql
INSERT INTO storage.buckets (id, name, public) VALUES ('job-photos', 'job-photos', true) ON CONFLICT (id) DO NOTHING;
CREATE POLICY "anon upload" ON storage.objects FOR INSERT TO anon WITH CHECK (bucket_id = 'job-photos');
CREATE POLICY "anon read"   ON storage.objects FOR SELECT TO anon USING (bucket_id = 'job-photos');
```

### Why slim view matters
Base64 photos in JSONB were downloaded on every 30–60s poll → 5GB/month free tier egress was exhausted within weeks. The `install_orders_slim` view strips `photos` from rooms in the poll response. Supabase Pro plan is now required (project upgraded 2026-07-01).

## Doc Scanner (added 2026-07-01)

CamScanner-style perspective-correcting document scanner, present in both `Site_Auditor_App.html` and `Site_Installer_App.html`. Activated by "📄 Scan" button in `addRoom()`.

### UX flow
1. Opens full-screen overlay with rear camera video feed
2. User taps "Capture" → freeze frame
3. 4 draggable corner handles appear over the captured image (auto-placed at 15% inset)
4. User drags handles to document corners
5. "⚡ B&W" toggle: grayscale + contrast enhance (optional)
6. "Apply" → homographic warp → output 800×(proportional) JPEG → saved to room photos

### Implementation
**Overlay HTML**: full-screen `#docScanOverlay` (z-index 300). Contains:
- `#dsCamState`: video `#dsVideo` + "Capture" button `#dsCaptureBtn`
- `#dsReviewState`: `#dsImgContainer` with `<img id="dsImg">` + SVG `#dsPolygon` + 4 `.ds-handle` divs (`#dsH0`–`#dsH3`) + footer with Retake/B&W/Apply buttons
- Hidden `#dsSrcCanvas` for capture

**Perspective math**: 8-parameter homography H solved via Gaussian elimination with partial pivoting. Applied as inverse mapping (output pixel → source pixel) with bilinear interpolation. `_dsComputeHomography(fromPts, toPts)` builds the 8×8 linear system.

**Handle drag**: pointer events on each `.ds-handle`. Each handle updates its corner in `_dsCorners[]`. SVG polygon redraws on each move.

**B&W enhance**: after warp, draws output canvas with `filter:'contrast(1.5) grayscale(1)'` and re-reads pixels via ImageData for pure B&W.

### CRITICAL: Lazy init pattern — NEVER use IIFE
```js
let _dsReady=false;
function _dsInit(){
  if(_dsReady)return; _dsReady=true;
  document.getElementById('dsCancelBtn').onclick=()=>_dsClose();
  // ... attach all overlay handlers ...
}
function openDocScanner(cb){
  _dsInit(); // safe: DOM is fully parsed by the time user taps Scan
  _dsCb=cb; ...
}
```
The overlay HTML appears **after** `</script>` in the document. If `_dsInit()` runs as an IIFE at script parse time, `getElementById('dsCancelBtn')` returns `null` → `.onclick` throws TypeError → the rest of the script (including `renderList()`) never runs → **entire app is dead on load**. Always use the lazy init pattern with the `_dsReady` guard. Do NOT convert to IIFE.

### Camera facing modes (both field apps)
| Context | Mode |
|---|---|
| Arrival confirmation photo | `facingMode:'user'` (front camera) |
| Job card room photos (`fileCam` input) | `capture='environment'` (rear camera) |
| Doc scanner | `facingMode:'environment'` (rear camera) |

## Slot System → Exact Time (updated 2026-07-01)
`o.slot` now stores `"HH:MM"` (24h) for new bookings. Legacy `sf1`/`sw1` IDs on existing orders still display correctly.

`SLOTS_FL` and `SLOTS_WP` arrays are still persisted to localStorage (used for backward-compat display and the "Slots & timings" config view).

| File | localStorage keys |
|---|---|
| SM_Audit_Dashboard | `md_audit_slots_fl`, `md_audit_slots_wp`, `md_audit_caps` |
| SM_Install_Dashboard | `md_install_slots_fl`, `md_install_slots_wp` |

### slotLabel(id) — all 4 files (function, not arrow)
- Legacy ID (`sf1`, `sw1`…): looks up `SLOTS_FL`/`SLOTS_WP` label
- `"HH:MM"` string: renders as `"10:30 AM"` (12h format)
- null/undefined: returns `"—"`

### SM Audit — booking
- `<input type="time" id="bookTime">` replaces old slot grid. `updateBookBtn()` syncs `draft.date`+`draft.slot` and enables "Book slot" button.
- **2-hour auditor buffer**: `auditorConflictOrder(aid, date, slotTime)` blocks assignment if the same auditor has a HH:MM booking within 120 min on that date. Buffer is **per-auditor** — other auditors are unaffected. Shows "has X:XX AM booking" in the auditor card.
- Legacy slot IDs bypass the conflict check (only HH:MM slots are compared).

### SM Install — booking (2-step flow as of 2026-07-01)
`renderAssignSection(o, sj, container)` uses a **2-step UX in standard mode**:
- **Step 1 — Book a slot**: date + start time → "Book slot" saves `sj.date`, `sj.slot`, `sj.status='scheduled'` to DB immediately. No installer required. `container._editingSlot` tracks edit state across re-renders. Auto-opens in edit mode when `sj.status==='reschedule'` or no date set.
- **Step 2 — Assign installer**: OPTIONAL — SM can close the drawer after Step 1 and return later to assign. Picker(s) show with no date fields (slot already locked from Step 1). "Save assignment" stamps `sj.date`/`sj.slot` onto each assignment and sets `sj.status='assigned'`.
- **Edit slot**: "Edit" button in Step 1 summary sets `container._editingSlot=true` and re-draws Step 1 form. Rescheduling auto-opens Step 1 in edit mode.
- **Status flow**: slot booked → `sj.status='scheduled'`; installer assigned → `sj.status='assigned'`. Parent order synced via `syncParent(o)`.
- **Custom / Multi-day mode**: keeps the old combined form (date per installer — 2-step doesn't apply here).
- `[data-time]` oninput still used in custom mode. Standard mode time captured at "Book slot" click from `#stepTime_{sjId}`.

### Field apps — autoFlip (both)
Parses HH:MM: `startH = h + m/60`. Legacy slot IDs still use `SLOTS[id].start`. "callpending" flip still 3h before start.

### Installer App — slotsLabel
No longer hardcodes `'Full day'` for flooring — reads `slots[]` so start time set by SM displays correctly.

## Field App SLOTS (Site_Auditor_App.html and Site_Installer_App.html)
Both field apps build their `SLOTS` lookup dynamically at startup (as of 2026-06-22):
```js
const SLOTS=(function(){
  const defFL=[{id:"sf1",label:"9 AM – 12 PM"},{id:"sf2",label:"12 PM – 3 PM"},{id:"sf3",label:"3 PM – 6 PM"}];
  const defWP=[{id:"sw1",label:"9 AM – 12 PM"},{id:"sw2",label:"12 PM – 3 PM"},{id:"sw3",label:"3 PM – 6 PM"}];
  let fl=defFL,wp=defWP;
  try{const sf=localStorage.getItem('md_XXX_slots_fl');const sw=localStorage.getItem('md_XXX_slots_wp');if(sf)fl=JSON.parse(sf);if(sw)wp=JSON.parse(sw);}catch(e){}
  const st=[9,12,15];
  const m={s1:{label:"9 AM – 12 PM",start:9},s2:{label:"12 PM – 3 PM",start:12},s3:{label:"3 PM – 6 PM",start:15}};
  fl.forEach((s,i)=>{m[s.id]={label:s.label,start:st[i]||9};});
  wp.forEach((s,i)=>{m[s.id]={label:s.label,start:st[i]||9};});
  return m;
})();
```
- Auditor app reads `md_audit_slots_fl` / `md_audit_slots_wp`; installer app reads `md_install_slots_fl` / `md_install_slots_wp`
- If the SM device and field worker device share the same browser, live-configured labels are used; otherwise hardcoded defaults apply
- `s1/s2/s3` retained for backward compat; `sf1/sf2/sf3` and `sw1/sw2/sw3` are the current standard IDs
- `start` hour is inferred from array position (index 0→9, 1→12, 2→15); custom slots beyond 3 default to `start:9`
- `autoFlip()` now handles both legacy slot IDs and `"HH:MM"` format (see note 6)
- **Installer app only**: `slotsLabel(j)` reads `j.slots[0]` via `slotLabel()` — returns e.g. `"10:30 AM"` for new bookings, joined labels for legacy multi-slot wallpaper (e.g. `'9 AM – 12 PM · 12 PM – 3 PM'`), or `slotLabel(j.slot)` fallback. **No longer hardcodes `'Full day'` for flooring.** Used in list card, detail subtitle, detail panel, and job card summary.

## Location Permission Enforcement (Site_Auditor_App.html and Site_Installer_App.html, added 2026-07-23)
Location is "somewhat mandatory" for field workers — see note 58. Identical logic in both files:
- **Periodic monitor** (`_geoPermStatus`, `_applyGeoPermState(state)`, `_locPermTick()`): `navigator.permissions.query({name:'geolocation'})` is queried once at boot and the returned live `PermissionStatus` object is cached; `_locPermTick()` re-reads its `.state` (a live-updating property, no new request) on every 30s poll tick and every `visibilitychange`-triggered refresh — piggybacked onto the existing `loadJobs()` cadence, no separate interval.
- **State `'prompt'` (undecided)**: `_applyGeoPermState` calls `navigator.geolocation.getCurrentPosition()` — the only way to make the native permission dialog reappear once dismissed without a decision. Once the worker answers, this stops firing for them.
- **State `'denied'`**: `#locDeniedBanner` is now `position:fixed;z-index:60` (was in-flow under `<header>` — invisible behind every full-screen `.screen` overlay before this fix) with `body.loc-banner-show` pushing `.app`/`.screen` down via `padding-top`/`top:46px`. Includes an "I've fixed it — Recheck" button (`#locRecheckBtn`) wired to `_locPermTick()` for instant feedback instead of waiting up to 30s. **Copy** (revised 2026-07-24 per user feedback — the original wording read as surveillance, not as a field-worker-facing tool): "Location access is blocked. Go to your phone settings → Site permissions → enable Location to confirm your site arrivals." — frames it around the specific job function (arrival verification) rather than "so the office can track you." If touching this banner again, keep that framing — simple, non-fear-inducing, tied to what the permission is actually used for.
- **Arrival-gate enforcement** (`openArrivalModal`'s `confirm.onclick`): if `capturedLat` is still `null` when the worker taps confirm, one last `getCurrentPosition` retry fires, then — if still null — `window.confirm(...)` (must be `window.confirm`, not bare `confirm` — that identifier is shadowed by the modal's own `confirm` DOM-element variable in this scope) warns that location is required and asks to continue anyway. Cancel keeps the modal open; accepting sets `locOverride:true`, passed through to the two call sites (`#atSite`/`#reached` handlers), which append `⚠ (no location — worker override)` to the log message — same pattern as the SM-override annotations in notes 52/55. **Camera-photo gating is unchanged** — this is an additive, logged soft-gate for location only, not a hard block.
- No new Supabase schema — rides entirely on the existing `log[]` jsonb shape.

## Architecture Patterns

### SM Audit Dashboard (`SM_Audit_Dashboard.html`)
- Nav views: Orders, Today's schedule, To reschedule, Follow-ups, **📅 Schedule**, Slots & timings, Auditors & caps, Deleted Orders, Rectifications. Plus an **INSIGHTS → 📉 Analytics** link (real `<a href="Admin.html">`, not an SPA view) added 2026-07-28 — see note 73.
- **Schedule tab** (replaced Availability calendar): 10-day view T−3 to T+6. Day columns show order count + up to 3 mini cards. Click a day → full detail list below. `calSelDay` state var. Wire handlers in `wire()`.
- **Poll query**: `audit_orders?select=id,pi,po,skus,bm,customer_name,phone,addr,status,service,slot,date,auditor_id,auditor_name,auditor_email,log,created_by_email&status=neq.deleted`
- `auditTicked` in mapRow is always `null` (not fetched in poll); `slotsForOrder(o)` falls back to `service`
- `CAPS[auditorId][date]` now saved to localStorage key `md_audit_caps`
- **📋 Pending POs import**: "Pending POs" button in Orders view header opens `kylasOverlay`. Fetches `POS_API?type=site_audit&page_size=100`. Deduplicates by `po_number` against `ORDERS[].po`. "Use this" pre-fills all Add Order fields (PI, PO, name, phone, address, BM) and shows `ao-kylas-note` banner.
- **Orders view state**: `filterStatus`, `filterDate` (YYYY-MM-DD or ""), `searchQ`. `setDateFilter(d)` sets `filterDate` and re-renders. Date picker in toolbar; resets to "" on nav switch. Filter: `if(filterDate && o.date !== filterDate) return false`.

#### slot_reserved orders in SM Audit (added 2026-07-06)
- CSS: `.c-slotres{background:#e8f0fb;color:#1a4a8a;}` — "Pre-booked (Store)" chip
- `STATUS` dict entry: `slot_reserved:{l:"Pre-booked (Store)",c:"c-slotres"}`
- `slotReservedDrawerBody(o)` renders a simplified drawer (no stepper, no auditor assignment) for pre-bookings from Store_Team_App
- Early return in drawer body: `if(o.status==='slot_reserved')return slotReservedDrawerBody(o);`
- Drawer footer: only Delete + Close buttons for slot_reserved (no scheduling controls)
- **"All" filter excludes `slot_reserved`**: these appear only in the "Pre-booked (Store)" filter tab
- Today's pre-booking banner shows count of today's `slot_reserved` orders across all stores
- **`slot_converted` status** ("Pre-booking Fulfilled" chip, `.c-completed`): what a pre-booking becomes after the SM clicks "✓ Service Created" (`markSvcCreated`) once the real Kylas service order exists — stops the pre-booking from double-counting a slot. The "Pre-booked (Store)" filter tab shows `slot_reserved` **and** `slot_converted` together (see note 70) — it's a permanent record of every store pre-booking, not just the still-open ones. `slotConvertedDrawerBody(o)` is the read-only view (Close-only footer, no Delete) with a best-effort "Linked service order" note (heuristic slot+date+phone/name match, no stored FK). The "Store pre-bookings" stat tile and today's banner still count `slot_reserved` only — they're the "what still needs action" nudge and must stay that way.

#### Auditor active_from management (added 2026-07-06)
- `loadAuditors()` fetches `active_from` from profiles: `profiles?role=in.(site_auditor,auditor_installer)&select=id,name,email,active_from`
- `capFor(aid, ds)` returns `0` if `ds < auditor.activeFrom` — auditor's daily cap is 0 before their start date
- Auditors & Caps view shows: green "Active" or amber "From [date]" chip per auditor; `data-activefrom` date input per auditor (blank = active now, date = available from that date)
- Cap cells before `active_from` are greyed out/disabled (auditor doesn't work those days)
- "Save" PATCHes `active_from` to Supabase profiles table — cross-device persistence (SM sets once, Store Team reads from DB)

### SM Install Dashboard (`SM_Install_Dashboard.html`)
- Nav views: Orders, **Need Action**, Call Operations today, Today's installs, To reschedule, Follow-ups, **📅 Schedule**, Slots & timings, Installers, Deleted Orders, Rectifications. Plus an **INSIGHTS → 📉 Analytics** link (real `<a href="Admin.html">`, not an SPA view) added 2026-07-28 — see note 73.
- **Schedule tab**: Same 10-day format. Uses `sjsForDay(ds)` which scans `sjEffectiveAssignments(sj)` — real `sj.assignments[]` (standard `.date` / custom `.dates[]`), else legacy `sj.installer`+`sj.date`, else (when a subjob is `scheduled` with a date but no installer yet) a synthetic **Unassigned** entry carrying `sj.date`/`sj.slot`. This surfaces scheduled-but-unassigned installs (2-step flow: slot booked in Step 1, installer assigned later in Step 2) on their booked day — see note 86.
- **Poll query**: `install_orders?select=*&status=neq.deleted`
- `loadDeletedOrders()` called on-demand; `loadOrders` + `loadDeletedOrders` both called after delete/restore actions
- **📋 Pending POs import**: identical structure to SM Audit Dashboard — same overlay (`kylasOverlay`/`kylasBody`), same dedup logic. Uses `type=installation`. Also pre-fills `ao-delivery` (delivery date) and SKU rows (`variant_handle` → code, product_name contains 'wallpaper' → type).
- **Orders view state**: `filterStatus`, `filterDate` (YYYY-MM-DD or ""), `searchQ`. `setDateFilter(d)` sets `filterDate` and re-renders. `installOrderHasDate(o, ds)` checks any subjob's assignments (standard `date`, custom `dates[]`, legacy `sj.date`). Date picker in toolbar; resets to "" on nav switch.
- **Mixed orders** (`isMixedOrder(o)` — both a flooring and a wallpaper sub-job on one order, added 2026-07-27, see note 71): delivery date, delivery-confirmation status, and the custom-wallpaper production tracker fork into fully independent per-sub-job tracks (installer/date already were independent via `subjobs[].assignments[]`). Single-category orders (~98% of live orders) are entirely unaffected. Rollup status `STATUS.mixed` ("Mixed — see details") shows on the main table when the two categories have diverged; drill into the order drawer for the real per-category detail.

### Site Auditor App (`Site_Auditor_App.html`)
- 3 screens: list view, detail screen, job card screen
- `ME = {name, email}` — polls `audit_orders` filtered by `auditor_email=eq.ME.email`
- **Poll query**: explicit column list without `audit_ticked`; `audit_ticked` fetched on-demand in `openJobCard` and PDF download
- `autoFlip()` flips `scheduled → callpending` 3 hours before slot start; guards against unknown slot IDs

#### Job Card Completion Flow (auditor app, new as of 2026-06-19):
1. Auditor fills room cards (photos, measurements, sketches)
2. Auditor clicks **"Proceed to client →"** → `showPassToClientAudit(o)`
3. **Handover screen**: "Please hand the phone to the client" with step list. Back = return to review.
4. **Terms & Conditions screen** (`showTCsAudit(o)`): T&C text (constant `MD_TC`) + mandatory checkbox. Agree → `signAndComplete(o)`.
5. **Ratings screen** (`signAndComplete(o)`): 3 mandatory questions (Q1 overall, Q2 auditor, Q3 cleanliness) + optional comments. All 3 required before proceeding.
6. **Signature screen** (`showAuditSignature(o, ratings)`): sign canvas + client name. "Generate PDF & complete".

#### Critical: autosave race condition fix (2026-06-23 — two-layer defence)
The seq counter alone is insufficient. The seq check runs before `await sbPatch(draft)`, but once the fetch is in-flight it cannot be cancelled. If the completion write resolves first and THEN the draft fetch resolves, the draft silently overwrites photos, sign, and ratings. Confirmed on ENQ2026062175756 (June 23): completion log entry at 07:14:21Z but DB still had `draft:true, photos:[]`.

**Layer 1 — early cancel**: `showPassToClientAudit(o)` immediately does `clearTimeout(_autosaveTimer);_autosaveTimer=null;_autosaveSeq++;` when the auditor hands the phone to the client. This prevents the 3s timer from ever firing during T&C/ratings/signature in the common case.

**Layer 2 — `_completionWrite` re-issue**: `let _completionWrite=null` declared at module level. In `finishAudit`, BEFORE the completion `sbPatch`, set `_completionWrite = completionPatch`. In autosave, AFTER `await sbPatch(draft)` resolves, check `if(_completionWrite)` — if set, immediately re-issue `sbPatch(jcOrder.id, _completionWrite)`. This repairs any overwrite regardless of network ordering.

**Do not remove either layer.** `clearTimeout` alone is broken. Seq counter alone is broken. Both layers are required.

### Site Installer App (`Site_Installer_App.html`)
- 4 screens: list view, detail screen, audit report, installation card
- **Poll query**: `install_orders?select=*&status=not.in.(pending,deliv_ontime,deliv_delayed,deleted)`
- `isPrimary` field: from `myAssign.primary === true`, or first assignment if no flag set

#### Job Card Completion Flow (installer app, new as of 2026-06-19):
Primary installer flow:
1. Fill room cards → **"Proceed to client →"** → `showPassToClientInst(j)`
2. **Handover screen** → **T&C screen** (`showTCsInst(j)`, constant `MD_TC_INSTALL`) → `signAndFinish(j)`
3. **Ratings screen** (`signAndFinish(j)`): Q1 overall, Q2 installer, Q3 cleanliness — all mandatory
4. **Signature screen** (`showInstallSignature(j, ratings)`) → save + PDF

Additional installer: marks own assignment complete via `markAdditionalComplete(j)` — no ratings/signature.

#### Critical: autosave race condition fix (2026-06-23 — same two-layer defence)
Same fix as auditor app. `showPassToClientInst(j)` does the early cancel. `_completionWrite` is set before `sbPatch` in `reallyDone`, storing `{subjobs, status:parentStatus, log:j.parentLog}`. Autosave re-issues after draft write. `markAdditionalComplete` also does early cancel. Installer autosave is more dangerous (read-modify-write: `sbGet` → mutate → `sbPatch`) — seq checks placed after `sbGet` and before `sbPatch`, plus `_completionWrite` catches the post-write race.

### Store Team App (`Store_Team_App.html`)
- Standalone slot-booking app for in-store staff at Material Depot's 7 experience centres: Bengaluru — JP Nagar, Whitefield, Yelahanka, HSR Layout, Basaveshwar Nagar; Hyderabad — Gachibowli, Kompally (city derived via `STORE_CITY`, note 88)
- **Open access** — no login required. Any visitor with the URL can use it. Store selection persisted in `localStorage` key `md_store` per device; first visit shows store picker overlay.
- **No `ME` session** — booking log uses `who:MYSTORE` and `created_by_email:'store-team'`. No sign-out button.
- **6 fixed slots**: 10:00, 11:00, 13:00, 14:00, 16:00, 17:00 (each 1 hour)
- **2-hour gap rule**: `slotsConflict(slotA,slotB)` — two slots conflict if gap between their start times is < 120 minutes in either direction
- **Availability**: AUDITOR_COUNT × cap per date, minus conflicting bookings. Active auditors filtered by `active_from ≤ date` (or null). Fetched from `profiles?select=id,active_from&role=in.(site_auditor,auditor_installer)`
- **Date selection**: 14-day strip (today + next 13). `dstr(d)` uses local date parts (`getFullYear/getMonth/getDate`) — **NOT `toISOString()`** which returns UTC and causes off-by-one at midnight IST
- **Pre-booking creates** `audit_orders` row: `{pi:'SRES-{storeCode6}-{timestamp9}', bm:MYSTORE, date, slot:slotId, status:'slot_reserved', skus:[{c:'AUDIT',n:'Site Audit',audit:true}], log:[{who:MYSTORE, ...}], created_by_email:'store-team'}`
- **Confirmed bookings section**: shows ALL non-deleted, non-slot_reserved audit orders for the selected date (all stores, all auditors). This is intentional — shows store staff total auditor workload for the day, which determines slot availability across all stores.
- **Poll**: no background poll — data re-fetched each time the date changes or booking is made
- `STORES` constant (note 88): `['JP Nagar','Whitefield','Yelahanka','HSR Layout','Basaveshwar Nagar','Gachibowli','Kompally']`, with a sibling `STORE_CITY` map (the single source of truth for store→city)

### Admin Console (`Admin.html`)
- **Mobile shell** (added 2026-07-23, see note 61): `@media(max-width:900px)` turns `.rail` into an off-canvas drawer (`#menuToggle` hamburger in header, `#railOverlay` backdrop, `closeMobileNav()`). Below 900px width, always test that any new full-width grid/table wraps instead of overflowing — `html,body{overflow-x:hidden}` is a safety net, not a substitute for wrapping content correctly.
- Nav views: Overview, Users, Role Viewer, **Job Overview**, Performance, **📉 Analytics**
- **`service_mgr` sessions, scoped to Analytics only** (added 2026-07-28, see note 73): the session guard now accepts `role==='service_mgr'` (previously hard-gated to `'admin'` only). For this role, `<body>` gets class `sm-analytics-only` (CSS: `body.sm-analytics-only .admin-only{display:none}`) which hides every `.admin-only`-tagged rail item/section — only Analytics remains. `navigate('analytics')` runs directly instead of `navigate('overview')`, and `#hdr-badge`/`#hdr-subtitle` are relabelled `ANALYTICS`/`Analytics`. Both SM dashboards link here via a plain `<a href="Admin.html">` in a new INSIGHTS rail group.
- **`store_staff` role** present in Admin UI (ROLES dict, ROLE_ICONS 🏪, ROLE_DESCS, overview stats tile, Add/Edit User modals, Role Viewer). CSS: `.rb-store_staff{background:var(--amberbg);color:var(--amber);}`. Routes to `Store_Team_App.html`. **Note**: Store_Team_App is now open-access — no login required — so creating `store_staff` profiles is no longer necessary for the app to work.
- **Job Overview**: `_loadJobsData(m)` + `renderJobs(m)` split — initial load shows spinner; thereafter a `_jobsPollTid` interval polls every 30s while the Jobs view is active (same cadence as SM Dashboard). Clears on nav away. Queries: `audit_orders?status=not.in.(deleted,slot_reserved)`, `install_orders_slim?status=neq.deleted` (switched from raw `install_orders` 2026-07-23, see note 58 — the slim view strips `subjobs[].jobcard.rooms[].photos`, avoiding the payload-size/timeout risk that caused installs to silently vanish). Install query uses `sbGetLong` (45s timeout, matches the Analytics tab's identical-shaped query) instead of `sbGet` (12s). `_realJobsAudit`/`_realJobsInstall` are separate last-known-good caches, each only replaced when its own fetch succeeds — a failed fetch no longer wipes the other entity type's rows. Installer email extraction reads `sj.assignments[].installer_email` (new format) with legacy `sj.installer_email` fallback.
  - **Header stat tiles are clickable filters** (`setJobStatFilter`, added 2026-07-29, see note 75): clicking Total Jobs/Active Now/Completed/Unassigned jumps straight to that exact row set, resetting any active date range/search first since the tile counts themselves are always computed over the unfiltered set.
- **Job Overview status labels**: `JOB_STATUS` dict includes `created`, `follow_up`, `call_na` in addition to the original set. Previously unknown statuses rendered with wrong chip style.
- **Job Overview** (merged Jobs + Job Cards): clickable table rows open a wide detail modal (`openJobDetail(pi, type)`). Modal fetches full order data on demand (including `audit_ticked`/`subjobs`). Shows rooms, measurements, photos (click to open full size), ratings (Q1+Q2+Q3 for both audit and install), signature. Download Job Card PDF button in modal. `genAuditPDF` and `genInstallPDF` now include Q3 in client feedback table.
- **Date range filter**: `jobsDateFrom`/`jobsDateTo` (YYYY-MM-DD or "") + `setJobDateFrom(d)`/`setJobDateTo(d)`/`clearJobDateRange()` (see note 65 — replaced the old single-date `jobsDateFilter` 2026-07-25). For audit jobs matches if `j.date` falls in `[from,to]`; for install jobs matches if any of `j.installDates[]` falls in the range. `getFilteredJobs()` is the single shared filter (type/status/date-range/search) — also feeds the CSV export. Date range inputs + a "⬇ Download CSV" button in the toolbar.
- **Analytics tab V2** (`renderAnalytics`, `drawAnalytics` + helpers `_anDstr`, `_anToIST`, `_anDateIST`, `_anMinsIST`, `_anParseDateText`, `_anInstallCanon`, `_anArrivalBucket`, `_anInstallArrivalStats`, `_anAuditArrivalStats`, `_anAttachAuditRatings`, `_anAttachInstallRatings`, `_anInstallerMap`, `_anAuditorMap`) — rewritten 2026-07-23 to fix the inconsistent-denominator bug across cards (see note 58); metric cards became clickable drill-downs with CSV export 2026-07-25 (see note 65); auto-refreshes every 60s while the tab is open, via `_anFetchData()` + `_anPollTid` 2026-07-27 (see note 69):
  - **Date range picker**: from/to date inputs + "Last 7/30/90 days" shortcuts. Defaults to last 7 days. `analyticsFrom`/`analyticsTo` module vars, applied via `drawAnalytics(m)`.
  - **Section order**: Site Audit renders first, then Site Installation (swapped 2026-07-23 — used to be install-first).
  - **Install section** — `_anInstallCanon(installs,from,to)` returns exactly **one row per subjob** in range (no more per-scheduling-attempt double-counting on reschedule). Membership test uses ONE current-relevant date per subjob: completion-log date if completed/partial (falling back to latest known scheduled date), else current scheduled date(s) — a subjob's OLD reschedule history never produces a second row. This single canonical list (`iCanon`) drives the header total, Completion %, status breakdown, delivery tracking, Material Depot Audit %, and the per-installer table — they can no longer drift apart.
    - Metrics: Installer Arrival On Time % (denominator = canonical rows with `status` completed/partial/**atsite** (note 74 — atsite jobs already have a real "arrived at site" log entry even though work isn't finished) AND a matching "arrived at site" log entry — matched against EITHER the row's display date or any of its `schedDates[]`, since arrival can land a day before the completion log is written on a multi-day job), Material Depot Audit % (phone match against all-time `audit_orders`, denominator = full canonical total, NOT distinct-PI count — this was already fully automated, no manual `service.audit_by` field is consulted), **Completion %** (completed+partial ÷ canonical total — replaces the old separate "Job Card & Signature %" card), NPS, Q1/Q2/Q3 (date-range scoped via completion-derived rating lookup, see below — NOT all-time despite what this doc previously said).
    - Live SM Need Action banner: ops calls due + overdue follow-ups + reschedule subjobs (live, NOT date-filtered).
    - Delivery tracking sub-row: log-mention delay count (`logDelay` — real scan of `o.log` for "delay") vs. confirmed delayed (`hasDelay`/`original_delivery_date` ≠ `delivery_date`) — see note 57, these used to silently compute the identical number under different labels.
    - Per-installer table: orders, completed, on-time arrival %, Q1/Q2/Q3 (date-range scoped, see below), quantity (WF boxes / WP rolls from `sj.items`).
  - **Audit section** — filtered by `o.date` in range (`aFiltered`), same canonical list drives the header total, Completion %, Reschedule Rate %, and the new Conversion % metric.
    - Metrics: **Completion %** (completed ÷ total — merged from the old separate "Job Card & Signature %" + "Completion Rate %" cards), Auditor Arrival On Time % (denominator = completed/**atsite** audits with a matching arrival log, note 74, same principle as install), Reschedule Rate %, **Site Audit → Order Conversion %** (new — % of distinct audit customers, by phone, in range who eventually placed an install order at ANY time, even after the range ends; phone-matched against the full all-time `installs` array, mirrors the Material Depot Audit % mechanism), NPS, Q1/Q2/Q3.
    - Per-auditor table: orders, completed, on-time arrival %, Q1/Q2/Q3.
  - **Arrival on time**: row-level match — an "arrived at site" log entry must belong to THIS specific row (matching actor name + date), not an independent scan of the whole raw array scoped only by a trackFrom/to date filter (that was the old mechanism and the reason its denominator never agreed with anything else in the section). Floor is still 2026-07-02. IST conversion: `+19800000 ms`. >3 min past scheduled slot = late.
  - **Ratings (NPS/Q1/Q2/Q3)**: attached via `_anAttachAuditRatings`/`_anAttachInstallRatings` — starts from the completed subset and looks up each one's rating (audit: by `order_id`, unambiguous; install: `order_id` on a rating row is the PARENT order id and does NOT disambiguate multiple subjobs on one order — confirmed directly in both field apps' `sbPost('ratings',...)` calls — so install additionally matches by assigned-installer `staff_email`, falling back to nearest-completion-timestamp for the residual same-installer-two-subjobs case). Replaces the old independent `ratings.created_at`-date filter, which only coincidentally lined up with the completed count and could cross-attribute a rating to the wrong sibling subjob under the same PI. Known limitation: the same-installer-two-subjobs edge case isn't always perfectly disambiguated — documented in the Analytics footer, not hidden.
  - **Data queries**: `install_orders_slim?select=id,pi,status,subjobs,service,delivery_date,created_at`, `audit_orders?select=id,pi,status,date,slot,log,auditor_name,auditor_email,created_at`, `ratings?select=order_type,pi,order_id,q1_score,q2_score,q3_score,created_at,staff_name,staff_email` — `id`/`order_id` added 2026-07-23 for the row-level rating joins above.
  - **Known limitation**: install order `phone` (used by Material Depot Audit % and Site Audit → Order Conversion %) is only populated for orders created on/after 2026-07-01 (merged in via a separate `installLogRes` query scoped to that date) — conversions/matches involving earlier install orders won't be detected. Documented in the Analytics footer.

## Ratings — Q1/Q2/Q3 Questions
| Question | Auditor app | Installer app |
|---|---|---|
| Q1 | Overall Site Audit experience | Overall site installation experience |
| Q2 | Site auditor and their behaviour | Site installer rating |
| Q3 | How clean did the auditor leave the site after the audit? | How clean did the installer leave the site after the installation? |

All 3 are mandatory (1–10 scale). Comments optional. Stored in `sign.ratings` and `ratings` table.

## Job Card Data Shapes

### Audit job card (`audit_ticked`)
```json
{
  "auditor": "Auditor Name",
  "date": "2026-06-19",
  "sign": {
    "img": "<base64 jpeg>",
    "name": "Client Name",
    "ratings": {"q1": 8, "q2": 9, "q3": 7, "comments": "Great service"}
  },
  "rooms": [{
    "name": "Master Bedroom", "type": "flooring", "sku": "WF-OAK-12MM",
    "calc": {"area": "180", "boxes": "12", "skirt": "40", ...},
    "notes": "Parquet pattern", "photos": ["<base64>", ...], "sketchStrokes": [[{x,y}, ...], ...]
  }]
}
```
Flooring calc: `area, boxes, skirt, skirtH, lprof, rprof, tprof, corner`
Wallpaper calc: `warea, rolls, repeat, match, adh, primer`

### Install job card (`subjobs[i].jobcard`)
```json
{
  "rooms": [{"name": "Living Room", "sku": "WF-OAK-12MM", "qty": "12 boxes", "height": "10 ft", "width": "12 ft", "photos": ["<base64>"], "comments": "..."}],
  "sign": {"img": "<base64>", "name": "Client Name", "ratings": {"q1": 9, "q2": 8, "q3": 7, "comments": ""}}
}
```

## JS Field ↔ DB Column Mappings

| JS (audit orders) | DB column |
|---|---|
| `o.name` | `customer_name` |
| `o.po` (array) | `po` (text, comma-joined) |
| `o.auditTicked` | `audit_ticked` (always null in poll, fetched on-demand) |
| `o.auditor` | `auditor_id` |
| `o.auditorName` | `auditor_name` |
| `o.auditorEmail` | `auditor_email` |

| JS (install orders) | DB column |
|---|---|
| `o.name` | `customer_name` |
| `o.matchedAudit` | `matched_audit` |
| `o.deliveryDate` | `delivery_date` |
| `o.customWp` | `custom_wp` |
| `o.customWpStage` | `custom_wp_stage` |
| `o.customWpMeta` | `custom_wp_meta` |
| `o.auditBy` | `service.audit_by` |

| JS (subjob / assignment) | field |
|---|---|
| `sj.assignments` | `assignments[]` |
| `sj.jobcard` | `jobcard` |
| `a.primary` | `primary` (bool — drives sj.status) |
| `a.installer_name` | `installer_name` |
| `j.isPrimary` | computed at loadJobs from `myAssign.primary` |

## CSS Design System
```css
--navy:#1F3A5F   --navy2:#16294a  --blue:#2E6CA8   --yellow:#F4C20D
--ink:#1b2230    --muted:#67748a  --line:#dde3ec   --bg:#eef1f6   --card:#fff
--green:#1f7a3f  --red:#b3261e    --amber:#9a6200  --purple:#5b3aa6
--teal:#0f6e74   (install dashboard + auditor_installer role colour)
```

Auditor app buttons: `.btn.green`, `.btn.navy`, `.btn.blue`, `.btn.ghost`, `.btn.warn`
Installer app buttons: `.bigbtn.green`, `.bigbtn.navy`, `.bigbtn.blue`, `.bigbtn.amber`, `.bigbtn.red`, `.bigbtn.ghost`
Admin analytics: `.an-section`, `.an-grid`, `.an-grid4` (4-col), `.an-card`, `.an-val`, `.an-bar`, `.an-stars`, `.an-nps-row`, `.an-sub-head`, `.an-inst-table`, `.an-deliv-row`, `.an-date-row`, `.an-date-inp`, `.an-apply-btn`, `.an-footer`
SM schedule calendar: `.calschedwrap`, `.caldays`, `.daycol`, `.daycol.sel`, `.daycol.today`, `.daycol.past`, `.calmini`, `.caldetail`, `.caldet-card`

## Status Chips
| Status key | CSS class | Display label |
|---|---|---|
| `pending` | `c-pending` | Pending |
| `created` | `c-created` | Service Created |
| `call_na` | `c-call_na` | Call not picked |
| `scheduled` | `c-scheduled` | Scheduled |
| `assigned` | `c-assigned` | Assigned |
| `callpending` | `c-assigned` | Call Pending |
| `reschedule` | `c-reschedule` | Reschedule |
| `onway` | `c-onway` | On the Way |
| `atsite` | `c-atsite` | At Site |
| `completed` | `c-completed` | Completed |
| `partial` | `c-partial` | Partially Completed |
| `deliv_ontime` | `c-deliv_ontime` | Delivery on time |
| `deliv_delayed` | `c-deliv_delayed` | Delivery Delayed |

## Important Implementation Notes

1. **Job key format in installer app**: composite key is `pi + '|' + sjId` — parsed with `key.indexOf('|')`, NOT `split('_')`

2. **Autosave draft excludes photos**: `collectRooms().map(({photos, ...rest}) => rest)` — photos stripped from draft saves; full photos only saved on final completion. The completion race fix uses TWO layers: (a) `showPassToClient*` cancels the timer early (`clearTimeout+seq++`) before the client flow; (b) `_completionWrite` module var stores the completion patch before `sbPatch` — autosave checks it after its own draft write and re-issues completion if set. Both `_autosaveSeq` AND `_completionWrite` are required. Do NOT remove either.

3. **audit_ticked never in poll**: SM_Audit_Dashboard and Site_Auditor_App use explicit `select=` without `audit_ticked`. It is fetched on-demand for job card screen and PDF. `o.auditTicked` is always `null` in the SM poll mapRow.

4. **Deleted orders on-demand**: SM dashboards no longer fetch deleted orders in the main poll. `loadDeletedOrders()` is called only when the "Deleted" tab is opened OR after a delete/restore action.

5. **Slot persistence**: SLOTS_FL, SLOTS_WP, CAPS are now saved to localStorage on every change (add/delete/save). Read on page init. Keys: `md_audit_slots_fl`, `md_audit_slots_wp`, `md_audit_caps`, `md_install_slots_fl`, `md_install_slots_wp`.

6. **Slot format is now HH:MM** (updated 2026-07-01): `o.slot` stores `"HH:MM"` (24h) for new bookings (e.g. `"10:30"`). Legacy IDs (`sf1`, `sw1`) on existing orders still display correctly. `slotLabel(id)` in all 4 files handles both formats — it is a function, not an arrow, and must remain a function. `autoFlip()` in both field apps parses HH:MM (`startH = h + m/60`) in addition to legacy SLOTS lookup. Do NOT revert `slotLabel` to a one-liner arrow or remove the HH:MM branch from `autoFlip`.

7. **Client flow is 3 mandatory steps**: T&C (checkbox required) → 3 ratings Q1+Q2+Q3 all required → signature. The field worker never sees these screens — they hand the phone to the client at the "Proceed to client" screen.

8. **T&C text as constants**: `MD_TC` in auditor app, `MD_TC_INSTALL` in installer app. Update these constants when MD provides final T&C text.

9. **NPS formula**: Promoters = Q1 ≥ 9, Detractors = Q1 ≤ 7 (not ≤6). Range: −100 to +100. Implemented in Admin.html `drawAnalytics`.

10. **Q3 DB column**: `ALTER TABLE ratings ADD COLUMN IF NOT EXISTS q3_score int CHECK (q3_score BETWEEN 1 AND 10);` — must be run in Supabase SQL Editor. Ratings write is non-blocking (`try/catch`) so the app won't break if the column is missing, but Q3 won't persist to the table.

11. **PDF client feedback table**: All PDF generators include 3 rows (Q1, Q2, Q3 scores + comments) when ratings are present. This covers: field apps (`genPDF` in both), Admin (`genAuditPDF`, `genInstallPDF`), and SM dashboards (`genAuditPDFSM`, `genInstallPDFSM`). The SM dashboard versions were missing this section and were fixed 2026-06-19.

12. **Admin job detail modal**: `openJobDetail(pi, type)` fetches full order on demand. Modal uses `.modal.wide` class (`max-width:720px`). Cleanup: `document.getElementById('modal').className='modal'` on close to reset width.

13. **Analytics proxy metrics**: Job Card Filling % and Signature % are calculated as unique PIs with ratings / completed orders — ratings are only written after the client signs, so a rating entry confirms both.

14. **Schedule calendar state**: `calSelDay` is a module-level `let` in both SM dashboards. It defaults to `dstr(today)` and persists during the session. Clicking a `.daycol` updates `calSelDay` and calls `render()`. Clicking `.calmini` mini-card stops propagation and opens the order drawer directly.

15. **Smart quotes in JS**: Never use typographic quotes (`"`, `"`, `'`, `'`) — only ASCII. The Edit tool can introduce smart quotes from markdown rendering.

16. **Backward compat for photos**: `r.photos || (r.photo ? [r.photo] : [])` handles both old single-photo and new photos-array formats in all PDF generators.

17. **Job composite key in installer app**: `pi + '|' + sjId` — use `indexOf('|')` to parse. Never use `split('_')` because underscores appear in PI numbers.

18. **Primary installer drives sj.status**: in `adv()`, `sj.status` is only updated when `amPrimary === true`. Non-primary: update own `assignment.status` only. In `showInstallSignature` (primary completion), `sj.status = 'completed'` unconditionally.

19. **opsCallDue covers deliv_delayed**: `opsCallDue(o)` checks `['pending','deliv_delayed'].includes(o.status)`.

20. **Repeated delivery date updates**: A `deliv_delayed` order can be delayed again any number of times. The drawer shows a date picker pre-filled with the current delivery date and a "Further delayed" button. The `#markDelayed` handler checks `wasDelayed = o.status === "deliv_delayed"` before updating status, and logs "Delivery further delayed. New date: …" vs "Delivery delayed — BM asked to inform client. New date: …" accordingly. `delivery_date` in the DB is overwritten each time.

21. **SM_Audit slotsForOrder fallback**: When `o.auditTicked` is null (not fetched in poll), `slotsForOrder(o)` falls back to `o.service.flooring/wallpaper` to determine FL vs WP slots correctly. Do NOT revert this fallback.

22. **SM Install PDF installer name**: `genInstallPDFSM` resolves the installer name via `sj.assignments` (new multi-installer format) first, falling back to legacy `sj.installer` UUID lookup and `sj.installer_email`. Never use `sj.installer` alone — it is not set in the current format.

23. **Field app slot labels are dynamic, not hardcoded**: Both field apps build `SLOTS` at startup by reading from the SM dashboard's localStorage keys. Do NOT revert to a static `const SLOTS = {...}` object. The installer app uses `slotsLabel(j)` (not `slotLabel(j.slot)`) everywhere time is displayed — with the new HH:MM format, `slotsLabel` reads `j.slots[0]` and returns e.g. `"10:30 AM"`. The installer app no longer hardcodes `'Full day'` for flooring; it reads the start time from `slots[]`. If you add new time-display locations: use `slotsLabel(j)` in the installer app and `slotLabel(o.slot)` in the auditor app.

24. **Swipe-back navigation blocked on all authenticated pages**: All four pages (SM_Audit_Dashboard, SM_Install_Dashboard, Site_Auditor_App, Site_Installer_App) run these two lines immediately after the session guard:
    ```js
    history.replaceState(null,'',location.href);
    window.addEventListener('popstate',()=>history.pushState(null,'',location.href));
    ```
    `replaceState` removes Login.html from the browser history stack. The `popstate` listener catches any back navigation attempt (two-finger swipe, browser back button) and immediately re-pushes the current page, keeping the user on the dashboard. Sign-out still works because it uses `window.location.href='Login.html'` directly. Do not remove these lines — without them, swiping back on a Mac touchpad navigates to the login page.

25. **Email restriction removed** (as of 2026-06-23): Login.html accepts any valid email format. Admin "Add New User" and both SM "Add Staff Member" forms validate format only. Access gated by `profiles` table. **Do not add back any domain restriction.**

26. **Login.html Supabase error vs. user-not-found** (fixed 2026-06-26): `trySend()` checks `!Array.isArray(rows)` and `rows.length===0` as separate conditions. Non-array → "Network error — please try again." Empty array → "This email isn't approved for access yet." Previously both showed the not-approved message, hiding real downtime from users.

27. **SM dashboards always render on load failure** (fixed 2026-06-25): `loadOrders()` calls `render()` in ALL paths — success, non-array response, and exception. `if(!Array.isArray(rows)){render();return;}` and `catch(e){if(!$("#main").innerHTML.trim())render();}`.

28. **Fetch timeout + connection banner + fast retry** (fixed 2026-06-29): `sbGet` uses `AbortController` with 12s timeout. SM dashboards have `_connErr`/`_retryTid`/`_setConnErr(v)` vars. On failure: red sticky banner (`#connBanner`, between `</header>` and `<div class="layout">`), 8s fast retry via `_retryTid`. Early `render()` before `Promise.all(...)`. **Do NOT remove `_setConnErr` or `_retryTid` guard** — prevents stacking timers on consecutive failures.

29. **Doc scanner `_dsInit()` must be lazy** (fixed 2026-07-01): `_dsInit()` must be called from inside `openDocScanner()` (with `_dsReady` guard), NEVER as an IIFE at script parse time. The overlay HTML is injected AFTER `</script>` — `getElementById` returns null at parse time → `.onclick = ...` throws TypeError → `renderList()` never runs → entire app dead on load. The `_dsReady` flag ensures init runs once on first scanner open, when DOM is guaranteed to be ready.

30. **ALL compress functions require crossOrigin + try-catch** (fully fixed 2026-07-04): Every compress function in every file must set `im.crossOrigin='anonymous'` BEFORE `im.src` for any https:// URL, AND wrap the entire `onload` body in `try { ... } catch(e) { resolve(null); }`. Without this, canvas is CORS-tainted → `toDataURL()` throws SecurityError inside the async handler → Promise never resolves → PDF hangs forever. File-by-function inventory:
    - `Site_Auditor_App.html`: `compressImg()` (used in genPDF) ✅, `compress()` (dead code) ✅
    - `Site_Installer_App.html`: `compress()` ✅
    - `SM_Audit_Dashboard.html`: `_compressSM()` ✅ (was missing — caused SM PDF hang on Storage URLs)
    - `SM_Install_Dashboard.html`: `_compressInst()` ✅ (was missing — same)
    - `Admin.html`: `_compressAdmin()` async helper ✅; `genAuditPDF` and `genInstallPDF` are now `async`, use `await _compressAdmin(url)` for all images, and use `for` loops instead of `forEach` to support `await`. Pattern: `if(dataUrl && dataUrl.startsWith('http')) im.crossOrigin='anonymous';` before `im.src=dataUrl`.

31. **Installer PDF always refetches from `install_orders`** (fixed 2026-07-01): The PDF `dlPdf` handler in `Site_Installer_App.html` must always fetch from the full `install_orders` table before calling `genPDF(j)`. The poll uses `install_orders_slim` which strips `photos` from rooms — `j.jobcard` is truthy (the jobcard object exists) but `j.jobcard.rooms[].photos` are all empty. The old `if(!j.jobcard)` guard is NOT sufficient; always refetch: `const rows=await sbGet('install_orders?id=eq.'+j.id+'&select=subjobs')` and splice in the fresh jobcard before generating PDF.

32. **Login reads DOM directly for Android autofill** (fixed 2026-07-01): `trySend()` in `Login.html` reads `const _ei=$("#email"); const email=((_ei&&_ei.value)||state.email||"").trim().toLowerCase()`. Android autofill fills the input's DOM `.value` property but does NOT fire `oninput` → `state.email` stays empty → the email regex fails → "Enter a valid email address." is shown even though the field looks filled. Reading `element.value` directly in the submit handler bypasses this.

33. **Supabase Pro plan required** (upgraded 2026-07-01): The project exceeded Supabase free tier (5GB/month egress) because base64 photos were stored in JSONB columns and downloaded on every poll. Supabase project `material-depot1/material-depot-site` has been upgraded to Pro. If egress concerns arise again, verify slim views are in use for polls and new photos are going to Storage (not base64).

34. **Status sync: order-level vs. subjob-level** (one-time repair 2026-07-01): `install_orders.status` (parent) must always be ≥ the subjob and assignment statuses. After a direct DB edit caused 3 orders (Abhijith, ravi, Srikant) to have `status=atsite` at order level but `assigned`/`scheduled` at subjob level, a one-time SQL repair was run to advance all lagging subjob/assignment statuses to match their parent. If this discrepancy recurs, diagnose with: `SELECT id,customer_name,status,subjobs FROM install_orders WHERE status IN ('callpending','onway','atsite') AND EXISTS (SELECT 1 FROM jsonb_array_elements(subjobs) s WHERE (s->>'status') NOT IN (status::text,'completed','partial','reschedule'));`

35. **Auditor app unscheduled section** (added 2026-06-26): `listView()` in `Site_Auditor_App.html` shows an amber "Awaiting schedule — no date set yet" section for `o.date===null` orders that aren't completed/reschedule. Mirrors `Site_Installer_App.html`.

36. **Date filter in orders views**: SM Audit: `filterDate` vs `o.date`. SM Install: `filterDate` via `installOrderHasDate(o, ds)` (checks all subjob assignment dates). Admin Job Overview: `jobsDateFrom`/`jobsDateTo` range vs `j.installDates[]` (see note 65 — was a single-date `jobsDateFilter` before 2026-07-25). All reset on nav switch.

37. **Search by Enquiry ID / customer** (added 2026-06-30):
    - **Admin Job Overview**: `jobsSearch` filters `realJobs` by `j.id` (PI) or `j.customer`. Input `#jobsSearchInput` first in toolbar. Focus/cursor restored after re-render. `navigate()` resets `jobsSearch=''`.
    - **SM dashboards**: `searchQ` already searches PI, customer, phone, BM, SKU in `ordersView()` — no change needed.

38. **Exact time slot system + 2-hour auditor buffer** (2026-07-01): `o.slot` stores `"HH:MM"` for new bookings.
    - **SM Audit**: `<input type="time" id="bookTime">`. `updateBookBtn()` syncs `draft.date`+`draft.slot`. `auditorConflictOrder(aid, date, slotTime)` blocks auditor if same auditor has HH:MM booking within 120 min on same date. Per-auditor only — Auditor B never blocked by Auditor A.
    - **SM Install**: `<input type="time" data-time="idx">` for standard wallpaper, standard flooring, custom wallpaper. `[data-time]` oninput sets `assigns[idx].slots=["HH:MM"]`. Custom flooring stays "Full day".
    - **Field apps**: `slotLabel` (function, not arrow) handles both HH:MM and legacy IDs. `autoFlip` parses HH:MM as `startH=h+m/60`. Legacy `sf1`/`sw1` orders unaffected.

39. **Activity log format + actor attribution** (finalised 2026-07-01): Log entries are `{t, d, by, who}` objects.
    - `t`: action text. `d`: ISO timestamp. `by`: `"manual"` or `"auto"`. `who`: actor name.
    - SM dashboards: every `o.log.push(...)` includes `who:SESSION.name`.
    - Auditor app: `o.log.push(...)` includes `who:ME.name`. Installer app: `j.parentLog.push(...)` includes `who:ME.name`.
    - **Display**: title line = `[Name in navy bold] · [action text]`. Sub-line = `[D Mon YYYY · HH:MM] · SM/installer/auditor`.
    - `fmtLog(d)` always returns `"D Mon YYYY · HH:MM"` — **NO "Today"/"Yesterday" labels ever**. Do not re-add those branches — was the recurring date-display bug.
    - Old entries without `who` render gracefully (actor prefix omitted).

40. **Photo handling in field apps — no crop, preserve aspect ratio** (fixed 2026-07-02): Both `Site_Auditor_App.html` and `Site_Installer_App.html` now use a shared `_resizeAndStore(dataURL, push)` helper for all photo additions. It resizes to max 1600px on the longest edge (preserving original aspect ratio, `image/jpeg` quality 0.88). Used for both camera (`addPhFromCam`) and gallery (`addPhFromGal`) — neither path goes through `cropModal`. The `cropModal` code is retained but no longer invoked during photo add. Thumbnail click (80×80 preview) now opens a full-size view-only overlay (dark backdrop, tap to dismiss) instead of the crop modal. **Do not reinstate `cropModal.open` in the photo add paths** — portrait photos were being cropped to a 340×255 landscape frame, losing top/bottom content.

41. **original_delivery_date field** (added 2026-07-02): `install_orders` table has a new `original_delivery_date` (text) column. Set to `delivery||null` at order creation in `addOrder()` (`SM_Install_Dashboard.html`). **Never updated when delivery is delayed** — `delivery_date` is overwritten on each delay, but `original_delivery_date` is immutable. Used in Admin analytics to confirm actual delivery delays: `confirmedDelayed = attempts where originalDelivery ≠ currentDelivery`. Supabase SQL to add: `ALTER TABLE install_orders ADD COLUMN IF NOT EXISTS original_delivery_date text;`

42. **PWA install banner — `pwa-install.js`** (added 2026-07-04): All 6 HTML files include `<script src="/pwa-install.js"></script>` before `</body>`. The shared script shows a fixed bottom banner prompting mobile users to install the app. Never shows if already running as standalone PWA (`display-mode: standalone` or `navigator.standalone`). ✕ button dismisses for the browser session (`sessionStorage._pwaDismissed`) — reappears on next tab/session if not installed. `appinstalled` event removes the banner automatically. Android: intercepts `beforeinstallprompt`, shows "Install" button → calls `event.prompt()`. iOS: shows immediately with "Tap Share → Add to Home Screen" text (no install API on iOS). Desktop: never shows (detects Android/iOS via user agent). Each page has its own `<link rel="manifest">` pointing to its role-specific manifest (e.g. `manifest_sm_audit.json`, `start_url: /SM_Audit_Dashboard.html`). All pages register the service worker (`sw.js`) for offline support.

43. **`dstr(d)` must use local date parts** (IST timezone fix — 2026-07-07): Any function that converts a JS `Date` to a `YYYY-MM-DD` string for DB queries must use `d.getFullYear()`, `d.getMonth()+1`, `d.getDate()` — **never `d.toISOString().slice(0,10)`**. At midnight IST (UTC+5:30), `toISOString()` returns the previous UTC day — so July 7 at 12:00 AM IST becomes `2026-07-06` in the query. SM Dashboard already used local parts; `Store_Team_App.html` had the bug and was fixed. **Pattern**: `function dstr(d){const y=d.getFullYear(),m=String(d.getMonth()+1).padStart(2,'0'),day=String(d.getDate()).padStart(2,'0');return \`${y}-${m}-${day}\`;}`.

44. **Store Team auditor count must filter active_from** (2026-07-07): `Store_Team_App.html loadDay()` fetches auditor profiles with `active_from` and counts only those where `a.active_from <= date` or `a.active_from === null`. Using `.length` of all profiles gave inflated availability counts (showed "4 of 4 auditors" when only 1 was active).

45. **Admin Job Overview 30s auto-poll** (2026-07-07): `_loadJobsData(m)` / `renderJobs(m)` split. `_jobsPollTid` interval polls every 30s while `currentView==='jobs'` and `!document.hidden`. Cleared in `navigate()` when leaving. Queries exclude `deleted` and `slot_reserved` (`status=not.in.(deleted,slot_reserved)` for audit, `status=neq.deleted` for install). `JOB_STATUS` dict includes `created`, `follow_up`, `call_na`. Installer emails read from `sj.assignments[].installer_email` (new format) with `sj.installer_email` legacy fallback. **Superseded 2026-07-23 — see note 58**: the install query moved to `install_orders_slim` and gained defensive per-entity caching.

46. **`submitAddUser()` / `submitEditRole()` in Admin require try/catch** (2026-07-06): Both functions are wrapped in try/catch to surface DB errors (e.g. role constraint violations). Without this, the button stays in "Adding…" / disabled state with no user feedback when `sbPost`/`sbPatch` throws.

47. **PWA — Store Team has its own manifest** (2026-07-06): `Store_Team_App.html` has `<link rel="manifest" href="/manifest_store_team.json">`. `manifest_store_team.json` exists at project root with `start_url: /Store_Team_App.html`. The Store Team app also includes `pwa-install.js` before `</body>`.

48. **Store Team slot IDs are exact times** (2026-07-06): Store Team uses `{id:'10:00', startMin:600, endMin:660}` etc. — the `id` field IS the slot value stored in DB as `o.slot`. `slotsConflict(slotA, slotB)` checks `|slotA.startMin - slotB.startMin| < 120`. Pre-booking PI format: `'SRES-' + storeCode.toUpperCase().slice(0,6) + '-' + Date.now().toString().slice(-9)`.

49. **`pwa-install.js` included in all 7 HTML files** (updated 2026-07-06): Store_Team_App.html was added as the 7th file — it also includes `<script src="/pwa-install.js"></script>`.

50. **`active_from` is cross-device via Supabase** (2026-07-06): SM sets auditor `active_from` in Auditors & Caps view → Save PATCHes it to `profiles` table. Store_Team_App fetches `profiles?select=id,active_from&role=in.(site_auditor,auditor_installer)` each time a date is selected. This ensures Store Team on any device sees the current SM-configured auditor availability.

51. **Store Team App is open access — no login** (2026-07-07): `Store_Team_App.html` has no authentication. `ME` session variable removed. `doLogin()`, `showLogin()`, `getSession()` functions removed. Login screen HTML removed. `startApp()` skips session check — shows store picker if no `md_store` in localStorage, otherwise loads day view directly. Header shows only store picker button (no sign-out). Booking log uses `who:MYSTORE` and `created_by_email:'store-team'`. The anon Supabase key already had write access to `audit_orders`, so no backend changes were required. Share URL: `https://material-depot-site.vercel.app/Store_Team_App.html`.

52. **Auditor 2-hour conflict is now an SM-overridable warning, not a hard block** (2026-07-11): `auditorConflictOrder()` (the 2-hour-gap detection function) is unchanged in its gap math, so do NOT weaken or remove it. (Correction, note 87: an earlier version of this note claimed `Store_Team_App.html` calls `auditorConflictOrder` for its availability counting — it does NOT; the store app has its own separate `slotsConflict`/`getAvailability`. The two apps share only the same 120-min proximity *semantics*, not the function. Note 87 later broadened the *status set* `auditorConflictOrder` scans, which is compatible with this note since it stays on the soft `.conflict`/override path, never the `full`/click-block path.) What changed is only in `SM_Audit_Dashboard.html`'s two auditor-picker lists (`#auditorList` for normal assignment, `#slotResAudList` for Store-Team pre-booking reservation): a `cx` conflict no longer sets the row's `full`/unclickable state — only the daily cap/quota (`capBlocked`) does that now. A conflicting row gets a `.conflict` (amber) CSS class and is still clickable; clicking it shows a native `confirm()` dialog naming the conflicting time and asking the SM to confirm ("Only do this if the two sites are close by"). Accepting sets `draft.auditorOverride=true`, which the assign/reserve handlers append to the activity log as `"⚠ assigned/reserved despite 2-hour scheduling conflict (SM override)"`. Declining leaves the row unselected. Do not re-fold `cx` back into the `full`/click-blocking logic — the whole point is the SM can knowingly override proximity-only conflicts while the cap limit remains a hard block.

53. **Sunday is blocked for Store Team booking only — SM dashboards deliberately allow it** (2026-07-11, corrected same day): Sunday is a company-wide day off, but Service Managers still need to be able to assign exceptional site audits/installations on a Sunday when the business calls for it. Only `Store_Team_App.html` blocks it: `initDateNav()` iterates up to 30 calendar days to collect 14 **non-Sunday** chips (it no longer stops at a flat 14-calendar-day window) — the store team never sees a Sunday option at all, and if "today" itself is a Sunday, `SELECTED_DATE` defaults to the next available (Monday) chip. **`SM_Audit_Dashboard.html` and `SM_Install_Dashboard.html` have no Sunday restriction** — `capFor()`, `dateRange()`, and every date picker in both dashboards accept Sunday same as any other day, on purpose. An earlier version of this note (and an earlier commit) added a hard Sunday block to both SM dashboards too; that was reverted per explicit user correction — do not reintroduce it.

54. **Install SKU items unified to a single "Area (sq.ft)" measurement — rolls are always derived, never entered** (2026-07-15): Root-caused via a live production data query (`install_orders?select=pi,custom_wp,subjobs`) that turned up real corrupted items — literal strings `"Rolls "`, `"Sq. ft "`, `"Roll"`, `" Rolls "`, even `"Rectification"` stored in what should have been a numeric wallpaper roll count, plus ~44% of all SKU items with a blank Product Name. Root causes and fixes:
    - **Ambiguous dual-field entry caused the garbage values.** The pre-this-fix UI had a separate free-text "Qty" input next to a "Rolls" input (and, briefly, a customWp-branched "Rolls" vs "Sq Ft" choice) — easy to fat-finger or mix up. Fixed by removing `rolls`/`qty` as directly-editable fields entirely. Every SKU item (flooring or wallpaper) now has exactly one numeric field: `it.sqft` ("Area of installation (sq.ft)" for flooring, "Area to be wallpapered (sq.ft)" for wallpaper). `SQFT_PER_ROLL=57` is a top-level constant in `SM_Install_Dashboard.html` (duplicated in `Admin.html` for analytics) — wallpaper roll count is **always** `Math.ceil(sqft/SQFT_PER_ROLL)` per item, computed on the fly wherever needed (`totalRolls(sj)`, the wp-dur-badge, `renderAssignSection`'s Step-1 hint, `Admin.html`'s per-installer `wpRolls`) — never stored as its own field, so it can never independently go stale. `_skuRowHtml()` shows a live-updating "= X roll(s)" hint under the wallpaper input as the SM types (`data-rollcalc` + the `oninput` handler in `renderSkuRows`), plus the field-side `Site_Installer_App.html` shows the same computed estimate to installers.
    - **`o.customWp` no longer affects SKU data entry at all** — that flag only matters for the unrelated Custom/Multi-day *scheduling* mode toggle inside `renderAssignSection`. Do not reintroduce a customWp branch into `_skuQtyField`/`_emptySkuRow`/`_skuRowHtml`/`totalRolls`/the wp-dur-badge — that branching is exactly what caused the confusion previously.
    - **New inputs reject non-numeric garbage on blur** (`data-numeric="1"` + the `onblur` handler in `renderSkuRows`) — clears the field and toasts `"Enter a number (e.g. 120)"` rather than allowing another corrupted string into the DB.
    - **Backward compat for existing corrupted/legacy items**: if `it.sqft` is empty but a legacy `it.qty`/`it.rolls` value exists AND is actually numeric, it's shown as a "Previously: N — e.g. 120" placeholder (reference only, never auto-filled as real data). If the legacy value is itself non-numeric junk, no hint is shown at all — showing "Previously: Rolls  rolls" back to the SM would just be more confusion, not less.
    - **Product Name was blank ~44% of the time** because it was never carried through from the Pending POs import — `usePORow()` had `sku.product_name` from the Kylas API available but only fed `variant_handle` (the code) into the "Add Order" modal, and `submitAddOrder()` set `n:c` (name = code, a throwaway placeholder). Fixed by adding a hidden `<input class="ao-sku-name">` per SKU row (populated by `usePORow()` from `sku.product_name`), carried through `submitAddOrder()` into `o.skus[].n`, and used as the Product Name prefill in the Service Creation draft init (`(s.n&&s.n!==s.c)?s.n:''`) — SM can still edit/correct it, but it now starts pre-filled when the data was available at import time.
    - **Also fixed**: `submitAddOrder()`'s SKU code/type pairing had an index-misalignment bug — `skuCodes.map(...).filter(Boolean).map((c,i)=>...)` filtered the code array first, which shifts indices whenever a middle row is left blank, misaligning every subsequent row's type (and now name) lookup. Fixed by pairing `{code, type, name}` together *before* filtering.
    - Do not re-add a free-standing `rolls` or `qty` input for wallpaper SKU rows, and do not make `it.rolls` an independently-editable/stored field again — the whole point of this fix is that there is only one authoritative number (`sqft`) per wallpaper item.

55. **Manual "Set status → Completed" override now requires a confirm + typed reason when there's no signed job card** (2026-07-17): `SM_Audit_Dashboard.html` and `SM_Install_Dashboard.html` both have a `[data-setstatus]` button set that lets an SM force any order straight to a status. Root-caused via a live DB query (not just code review — see [[project_material_depot_site]] for the general "query prod first" pattern in this app): **~16% of "completed" site audits (17/104) and a comparable share of "completed" install subjobs (17/113) had no signed job card at all** — no photos, measurements, signature, or rating — because an SM had used this button to force-close them, confirmed via activity-log entries like `"Status set to Site Installation Completed (manually)"`. Some of these abandoned a real in-progress draft (auditor had measured 1–6 rooms via autosave, then the SM's override made the order vanish from the auditor's active list before they could finish signing).
    - **Fix (both dashboards)**: before setting `st==='completed'` when the order/subjob doesn't already have a signed job card (audit: `audit_ticked.sign` truthy and not `.draft`; install: per-subjob `sj.jobcard.sign`), show `confirm()` naming exactly what's missing (and, on the audit side, whether there's an orphaned draft with N rooms), then `prompt()` for a required reason. Cancelling either dialog aborts the whole status change — nothing is written. Confirming appends `· ⚠ forced Completed without signed job card: "<reason>" (SM override)` to the log entry, mirroring the existing 2-hour-conflict override pattern (note 52).
    - **Install-side cascade bug, fixed in the same change**: the pre-fix code cascaded ANY `st` in `FIELD_STATUSES` (including `'completed'`) to every subjob that wasn't already `'completed'`, regardless of whether that subjob had ever been touched. Concretely: closing a genuinely-completed wallpaper subjob also silently force-completed an untouched flooring subjob on the same order (confirmed on `ENQ2026062876897`). `st==='completed'` is now handled in its own branch, separate from the other `FIELD_STATUSES` cascades (which are still safe to auto-cascade since they don't fabricate a false "done").
    - A remediation worklist of the 34 pre-existing affected orders (customer name/phone/BM per record, grouped by how recoverable each is — orphaned draft / job-card-minus-signature / no-job-card-at-all / pre-job-card-feature legacy tag) was generated and handed off separately; it isn't tracked in this repo.
    - Do not remove the confirm/prompt guard or relax it back to a silent one-click override — that's the entire point of this fix. **Update (note 85, 2026-07-31)**: the inline `prompt()` for the reason was replaced by the shared `requireNote()` helper — the force-Completed path now sets a `forcedCompletedNoCard` flag and reuses the single mandatory note as the override reason (same `... forced Completed without signed job card: "<note>" (SM override)` log format), so there is exactly one prompt, not two. The confirm() and the not-written-on-cancel behaviour are unchanged.

56. **Admin Analytics — Site Installation Overview: Installer Arrival On Time % replaces Delivery Delay %** (2026-07-18): the old first tile (`Delivery Delay %`, driven by `iDelayed`) was redundant with the more detailed "Delivery Date Tracking" sub-row below it. `Installer Arrival On Time %` already existed in the grid (4th tile, mirroring `Auditor Arrival On Time %` in the Audit section) but wasn't in the prominent first slot. Swapped instead of showing both — `pctCard('Installer Arrival On Time %', iArrTot.onTime, iArrTot.onTime+iArrTot.late, ...)` now sits first; the duplicate 4th-tile copy was removed. `Material Depot Audit %` (phone-number match against all-time `audit_orders`) is unchanged and was verified against live data to already work as intended — no formatting-mismatch bug in phone numbers (161/163 install and 155/157 audit phones are clean 10-digit strings; normalizing didn't change the match count).

57. **Admin Analytics — Delivery Date Tracking row had two tiles silently computing the identical number** (2026-07-18): `iDelayed` (labelled "Delay event in log") and `confirmedDelayed` (labelled "Confirmed delayed (new)") were both driven by the exact same boolean — `original_delivery_date && delivery_date && original !== current` — despite this row being documented (see the Analytics tab V2 section above) as a comparison between a *loose* log-text signal and a *strict* date-field signal. The log-based half was never actually implemented; it just reused the date-comparison flag under a misleading label. Fixed by adding a real field, `logDelay:(o.log||[]).some(l=>l.t&&/delay/i.test(l.t))`, computed in `_anInstallAttempts` (renamed `_anInstallCanon` 2026-07-23, see note 58 — the field itself is unaffected) (only populated for orders created ≥ 2026-07-01, since `o.log` is fetched via a separate lightweight query scoped to that date — see `renderAnalytics`'s `installLogRes` fetch). `iDelayed` now filters on `logDelay`; `confirmedDelayed` filters on the existing `hasDelay` field (unchanged logic, just de-duplicated from a second inline copy of the same expression). Tiles renamed to `"Delay mentioned in log"` / `"No delay mentioned"` / `"Confirmed delayed (date changed)"` for clarity. Verified live over a 90-day range: 8 vs 7 — a real, previously-invisible gap. Do not collapse these back into a single shared boolean — the entire point is to catch cases where ops logged a delay but never updated the tracked date, or vice versa.

58. **Admin Analytics — full rewrite to fix inconsistent denominators across cards; `_anInstallAttempts` renamed `_anInstallCanon`** (2026-07-23): user reported the same date range showing different "total" counts on different cards in the same section (Site Audit: 24/29/31 for what should've been one range; Site Installation: 33/18/17/36). Root-caused to five disconnected date/field scopes across ~28 metric computations (audit's own `o.date`, install's per-scheduling-attempt model with reschedule double-counting, an independently-scoped arrival-log scan over the raw unfiltered arrays, `ratings.created_at` joined only by loose PI-string match, and an all-time-unscoped set for the phone-match metrics) — see the Analytics tab V2 section above for the full before/after. Key changes:
    - `_anInstallAttempts` → `_anInstallCanon(installs,from,to)`: now returns exactly one row per subjob (previously could return 2+ rows for the same subjob if a reschedule left multiple historical dates inside the range). Every subjob row also carries `schedDates[]` (all currently-known scheduled dates, independent of whether a completion log was found) — needed because on a genuinely multi-day job the installer's arrival can be logged a full day before the "installation completed" log entry that determines the row's display date; matching arrival strictly against that display date alone caused real false negatives in production (confirmed: `ENQ2026070778463`, arrival logged 2026-07-17, completion logged 2026-07-18 — the arrival-matching function must accept either date).
    - Arrival On Time % (both sections) now matches an "arrived at site" log entry against the SPECIFIC row it's being computed for (date + actor name), not an independent scan of the whole raw array bounded only by a separate trackFrom/to filter — its denominator is therefore always a proper subset of the same canonical/filtered list every other card in the section uses. Real production data still legitimately excludes some completed/partial jobs from this denominator (no arrival log written at all, or the log's `who` field doesn't match any of the row's assigned installers, e.g. a data-quality issue where a different name was logged than who was actually assigned) — that's expected, not a bug, and matches the metric's stated definition ("only jobs that actually reached site").
    - "Job Card & Signature %" removed from both sections; merged into a single **Completion %** card (audit: completed ÷ total; install: completed+partial ÷ canonical total).
    - Material Depot Audit % denominator changed from distinct-install-PI count to the full canonical total. The phone-matching itself was already correct/automated before this change — no manual `service.audit_by` field has ever been read by this tab.
    - New **Site Audit → Order Conversion %** card (Site Audit section): distinct audit-customer phones in range that appear anywhere in the all-time `installs` array, including install orders created after the range ends.
    - Ratings (NPS/Q1/Q2/Q3, both the Overview cards and the per-installer/per-auditor tables) now attach to each completed order via `_anAttachAuditRatings`/`_anAttachInstallRatings` — starting from the completed subset and looking up its rating, rather than independently filtering the `ratings` table by `created_at` and joining back by loose PI match. Audit ratings join by `order_id` (unambiguous — confirmed via `Site_Auditor_App.html`'s `sbPost('ratings',{...order_id:o.id...})`). Install ratings' `order_id` is the PARENT `install_orders.id` (confirmed via `Site_Installer_App.html`'s `sbPost('ratings',{...order_id:j.id...})` where `j.id` is `r.id` from `loadJobs()`, not the subjob id) — it does NOT disambiguate which of 2+ completed subjobs on one order a rating belongs to, so install additionally matches by assigned-installer `staff_email`, falling back to nearest-completion-timestamp for the residual same-installer-two-subjobs case. This is a real, permanent data-model limitation (documented in the Analytics footer), not something fixable purely client-side.
    - Site Audit section now renders above Site Installation (previously the reverse).
    - Do not reintroduce the per-scheduling-attempt reschedule-doubling model, an independently-date-scoped ratings filter, or a distinct-PI denominator for Material Depot Audit % — each was the specific root cause of one of the reported inconsistencies.

59. **Admin Job Overview — installation orders vanishing a few seconds after picking a date filter** (2026-07-23): root-caused to `_loadJobsData`'s install query fetching raw `install_orders` (full `subjobs` including embedded base64 room photos) via the short 12s-timeout `sbGet`, instead of the photo-stripped `install_orders_slim` view this codebase already built and uses elsewhere (Analytics tab, `SM_Install_Dashboard.html`, `Site_Installer_App.html`) for exactly this payload-size reason. `sbGet`/`sbGetLong` call `r.json()` unconditionally regardless of HTTP status, so a slow/erroring response resolves as a non-array object rather than throwing; the old code's unconditional `realJobs=[]` reset, followed by an `if(Array.isArray(installRes))` guard with no `else` branch, meant a single bad poll tick silently wiped every install row (of every date, not just the selected one) while the much lighter audit query kept succeeding — presenting exactly as "installs disappear a few seconds after I pick a date" (the "few seconds" was really just residual time until the next 30s poll tick, which starts counting from tab entry, not from when the date was picked). Two-part fix:
    - Install query switched to `install_orders_slim` (same column list) and moved from `sbGet` (12s) to `sbGetLong` (45s), matching the Analytics tab's identical-shaped query.
    - `_loadJobsData` now maintains separate `_realJobsAudit`/`_realJobsInstall` caches; each is only replaced when its OWN fetch actually returns an array. `realJobs` is always rebuilt as their concatenation, so a failed fetch for one entity type can never wipe the other (or its own prior successful data). A failure now also `console.error`s every time and toasts once (not repeated every poll tick while an outage persists, via `_jobsAuditFailed`/`_jobsInstallFailed` edge-triggered flags).
    - Do not reintroduce the unconditional `realJobs=[]` reset or let this query regress back to raw `install_orders` — both were direct causes of this bug.

60. **Location permission made "somewhat mandatory" for field workers** (2026-07-23, both `Site_Auditor_App.html` and `Site_Installer_App.html` — see the Location Permission Enforcement section above for the full mechanism): previously, geolocation permission was checked exactly once at page load with no periodic re-check and nothing gating any action on it — a denied/undecided permission was never actively surfaced again. Now: (a) a `_locPermTick()` re-check is piggybacked onto the existing 30s poll + `visibilitychange` listener (no new interval); (b) while permission state is `'prompt'`, `getCurrentPosition()` is called on every tick specifically to make the native browser dialog reappear — this is the only state where JS can do that; (c) `#locDeniedBanner` was switched from in-flow-under-`<header>` (invisible behind every full-screen `.screen` overlay — i.e. invisible for most of a shift) to `position:fixed;z-index:60`, with a manual "I've fixed it — Recheck" button; (d) `openArrivalModal`'s confirm handler now warns via `window.confirm(...)` — **must be `window.confirm`, not bare `confirm`, because `confirm` is already a local variable bound to the `arrConfirmBtn` DOM element in this function's scope; calling bare `confirm(...)` throws "confirm is not a function"** — if location wasn't captured, letting the worker proceed only via an explicit, logged override (`locOverride:true` → `⚠ (no location — worker override)` appended to the log entry, same annotation pattern as the SM-override notes above). Camera-photo gating is completely unchanged — deliberately a soft, logged gate for location only, chosen over a hard block so a genuine GPS/device permission quirk can't strand a worker mid-shift. Do not re-introduce a bare `confirm(...)` call inside `openArrivalModal`, and do not make this a hard block on `arrConfirmBtn` without discussing the trade-off first.

61. **Admin Console had no mobile-responsive layout at all — sidebar squeezed content, and a hardcoded stat grid caused an iOS scroll-lock bug** (2026-07-23): a user screenshot from an iPhone showed the fixed 210px `.rail` sidebar and `main` content squeezed side-by-side with no breakpoint, clipping the search box and wrapping filter pills into an unusable column. Root cause: `Admin.html` had exactly one media query in the whole file (`@media(max-width:780px)`, only for the Analytics `.an-grid`/`.an-grid4` column counts) — the sidebar/main shell itself was never made responsive. Fixed with a `@media(max-width:900px)` breakpoint: `.rail` becomes a `position:fixed` off-canvas drawer (`transform:translateX(-100%)` → `.rail.open{transform:translateX(0)}`), toggled by a new `#menuToggle` hamburger button in the header (hidden on desktop) and a `#railOverlay` dimming backdrop; `closeMobileNav()` is wired into both the overlay's click handler and every `.nav-item` click so picking a page auto-closes the drawer. `main` needs no explicit width rule — once `.rail` is taken out of flow via `position:fixed`, it's the only flex child of `.layout` and fills the screen automatically.
    - **Separate, more serious bug found in the same investigation**: the user separately reported "unable to scroll" on the real device — root-caused to `drawJobs()`'s Job Overview stats row (`Admin.html` line ~774) using an inline `style="grid-template-columns:repeat(4,1fr)"` instead of the shared, already-responsive `.stats{grid-template-columns:repeat(auto-fill,minmax(155px,1fr))}` class. `1fr` tracks don't shrink below a grid item's intrinsic min-content width unless the item itself allows it (`min-width:auto` is the CSS Grid default) — the 4 stat numbers (e.g. "319", "244") were wide enough that the row overflowed the viewport horizontally instead of wrapping. On iOS Safari, page-level horizontal overflow is what caused the "stuck, can't scroll" symptom — the browser has to arbitrate between horizontal/vertical scroll gestures once content is wider than the viewport, and vertical scroll can appear to lock up. Fixed three ways: (a) the inline style now reads `repeat(auto-fit,minmax(130px,1fr))` so it wraps into 2×2 on narrow screens like the shared `.stats` rule already does elsewhere; (b) `.stat-card` gained `min-width:0;overflow:hidden` as a defensive fix for this exact CSS Grid trap; (c) `html,body{overflow-x:hidden;max-width:100%;}` added as a page-level safety net so a similar future overflow bug degrades gracefully instead of locking scroll again.
    - Do not reintroduce a hardcoded fixed-column-count inline grid style anywhere in this file without also either using `auto-fit`/`auto-fill` or verifying the content wraps at ~350-390px (iPhone SE/standard width) — this exact bug (content-driven grid-track overflow, not a layout-width bug) is easy to reintroduce by copy-pasting a "4 stat cards in a row" pattern from a desktop-only context.

62. **Custom Wallpaper Production Tracker** (`SM_Install_Dashboard.html`, added 2026-07-23): custom_wp orders previously had no representation of the pre-delivery production journey (order placement -> vendor draft/render generation -> customer approval, with a revision loop -> arrival at warehouse) described in `Custom Wallpaper Order Workflow.pdf` — a `custom_wp` order started straight at `pending`/`deliv_ontime` as if the physical roll already existed. The existing delivery/delay tracking (`deliv_ontime`/`deliv_delayed`, `original_delivery_date`, `markDelayed`) and the full installer job-card flow already implement the back three of the PDF's 7 stages (Delivery Schedule, Delivery, Installation) and were **not** touched by this change.
    - **New fields**: `custom_wp_stage` (text, one of `draft` (default) -> `approval` -> `warehouse`) and `custom_wp_meta` (jsonb: `{vendor, renderUrl, renderIsImage, revisionCount, revisionNotes:[], warehouseReceivedAt}`). Only meaningful when `custom_wp=true`. "Order Placement" (the PDF's stage 1) has no stored value at all — it's always rendered as done the moment the order exists, since there's nothing for the SM to click for it.
    - **New drawer section** `customWpProductionSection(o)` (rendered via `if(o.customWp)html+=customWpProductionSection(o);` in `drawerBody`, placed before the existing Delivery section since production precedes delivery scheduling in the real workflow) shows a 7-node stepper (`cwpTrackHtml`/`CWP_STAGES`) spanning all 7 PDF stages: the first 3 dots (`draft`/`approval`/`warehouse`) are driven by the new stored stage; the last 3 (`delivsched`/`deliv`/`install`) are **derived read-only** from fields that already exist and already drive the rest of this dashboard (`cwpBackHalfDone`: delivery-scheduled once `deliveryDate` is set, delivered once `status` is past `deliv_ontime`/`deliv_delayed`, installed once every subjob is `completed`) — no new logic was added for those three, deliberately, so the new tracker cannot drift from or interfere with the existing state machine.
    - **Draft stage** action UI: vendor picker (`Macro Media`/`Life N Colors`/`Other`, shows the Macro Media 1 PM cutoff rule from the PDF as an inline note) + a render field that accepts **either** a pasted URL or an uploaded image. Uploads go through a new `_uploadRenderImage(dataURL)` helper that reuses the **same** `job-photos` Storage bucket/POST pattern the field apps' `_uploadPhoto` already uses (documented in the Photo Storage section above) — no new bucket/policy required. "Mark render ready" moves the order to `approval`.
    - **Approval stage**: shows the current render (image thumbnail or link) + vendor + revision count. "Approved by customer" advances to `warehouse`. "Revision requested" prompts for a required note (stored in `revisionNotes[]`, `revisionCount` incremented, `renderUrl` cleared), sends the stage back to `draft` so a new render can be entered — this loop can repeat any number of times, matching the PDF's approval cycle.
    - **Warehouse stage**: "Received at warehouse" sets `custom_wp_meta.warehouseReceivedAt` (a timestamp, not a new stage value) — this is the terminal signal for this tracker; the section then renders a collapsed "done" summary and the SM proceeds using the pre-existing Delivery section directly below, completely unchanged.
    - Every transition is logged into the existing `o.log[]` array using the standard `{t, d, by, who}` shape (note 39) — no parallel logging system was introduced.
    - **Deliberately informational, not a gate**: reaching `warehouse` does NOT block or require anything before the SM can set/confirm a delivery date — the existing Delivery section and `[data-setstatus]` override menu work exactly as before regardless of `custom_wp_stage`.
    - **`Admin.html`** (`openJobDetail`) shows the same 7-stage stepper read-only (no action buttons) since it already fetches `select=*` on the order. `install_orders_slim` (`docs/supabase_slim_views.sql`) was extended to include `custom_wp_stage`/`custom_wp_meta` so Admin Job Overview, Site_Installer_App, and Analytics polls keep seeing them (though only Admin's detail modal actually reads them).
    - Do not add a hard gate between `warehouse` and the delivery flow, and do not build out the PDF's full 22-status granular list (page 3) — both were explicitly decided against when this feature was scoped.

63. **Site audit type (`service.audit_by`) is now auto-detected by phone match, not manual-only** (`SM_Install_Dashboard.html`, added 2026-07-23): previously every install order required the SM to manually click "Material Depot audit" or "Customer self-audit" in the drawer (`setAuditBy`) — a live query found 164 of 183 non-deleted install orders (90%) had never had this set at all, showing "Not set" everywhere including on completed orders. The signal to determine it automatically already existed and was already trusted: Admin.html's Analytics tab has computed "Material Depot Audit %" since note 56 via an exact phone-number match against `audit_orders` (no normalization needed — verified live that normalizing phone formats doesn't change the match count) — that computation was read-only for reporting and never written back to the order itself.
    - **New helper** `detectAuditBy(phone)` in `SM_Install_Dashboard.html`: a live `audit_orders?select=id&phone=eq.<phone>&status=neq.deleted&limit=1` lookup at the moment of order creation — returns `'material_depot'` if any non-deleted audit order shares the phone, `'customer'` if none does, or `null` if the lookup itself fails (never guesses a value when the check couldn't run). Called from `submitAddOrder()` (covers both the manual "+ Add New Order" modal and the Pending POs import flow, which both funnel through this same function) — the result is written into `service.audit_by` at creation time, and a `by:"auto"` log entry records which way it went and why. The existing manual buttons are unchanged and still let an SM correct it.
    - **One-time backfill** (run 2026-07-23, script not part of this repo — one-off maintenance, not app code): for all 164 pre-existing orders with `service.audit_by` unset, ran the same phone-match logic once and PATCHed each order's `service.audit_by` + appended a `[backfill]`-tagged log entry. Result: 72 → `material_depot`, 92 → `customer`, 0 errors. **Deliberately did not touch the 19 orders that already had a manual value set** — a live comparison before backfilling found 6 of the 10 orders manually marked `material_depot` did NOT have a matching non-deleted `audit_orders` phone (likely audits done before this app existed, done under a different number, or whose audit record was later deleted) — so phone-matching is a good default, not perfect ground truth, and should never silently overwrite an SM's explicit manual call.
    - Do not extend this into a recurring background reconciliation job (e.g. re-checking already-set orders, or newly-created audit orders retroactively updating older install orders) — out of scope; the SM's manual buttons remain the correction path for edge cases.

64. **Reopening an already-signed job card used to silently destroy the signature the instant autosave fired** (`Site_Auditor_App.html` + `Site_Installer_App.html`, fixed 2026-07-25): root-caused live on `ENQ2026072381434` — auditor Anil Chakali fully completed, signed, rated, and downloaded a PDF for a site audit on 2026-07-24 (log confirms `"Site audit completed · JobCard signed"`), but an admin manually reset the order's status backward to "Site Auditor Assigned" the same day via `SM_Audit_Dashboard.html`'s `[data-setstatus]` override. This didn't touch `audit_ticked` itself, but it put the order back in the auditor's active list. The auditor revisited the site the next day and reopened the job card; `autosave()` unconditionally `PATCH`es the **entire** `audit_ticked`/`sj.jobcard` field with `{draft:true, rooms:...}` on every keystroke/photo/room edit — with no check for whether a signed completion already existed — so the first edit wiped the signature, ratings, and finalized room data with zero trace. This is a general, recurring failure mode, not specific to this one order: **any** reopen of a completed job card (via the auditor app's own "Edit / Add Rooms" button on a completed order, or via an SM/admin moving status backward) triggers the same silent destruction the moment autosave fires.
    - **Fix — both field apps**: `openJobCard()` now always re-checks the server for an existing signed record first (`audit_ticked.sign`/`sj.jobcard.sign` truthy and not `.draft`) rather than trusting a possibly-stale local/in-memory copy. If found, the job card opens in a visibly-flagged "already signed" state (`jcHadSign=true`, amber banner, a log entry `"Reopened signed job card for edit — signature kept on file until re-signed"`), and `autosave()` refuses to write the draft-overwrite PATCH to the DB while `jcHadSign` is true — edits are kept `localStorage`-only (`showSaveStatus('local')`) until the worker actually completes the full re-sign flow (T&C → ratings → signature), which writes the real replacement via the existing, unaffected completion path. The installer app's `openJobCard` was also changed to always re-fetch `subjobs` fresh from `install_orders` (rather than trusting `j.jobcard`), since `jcBack`'s `j.jobcard={rooms:collectRooms()}` mutation would otherwise strip the in-memory `.sign` marker.
    - **Companion guard — SM dashboards**: `SM_Audit_Dashboard.html`'s override now shows an informational confirm when moving an order backward off `completed` while a signed `audit_ticked` exists, explaining the field app will let the auditor redo it (data is now protected, but the SM should know). `SM_Install_Dashboard.html`'s `st==='created'` cascade — which unconditionally reset every subjob's status/assignments including already-signed ones — now asks whether to keep signed subjob(s) untouched instead of wiping them by default.
    - Do not remove the `jcHadSign` guard or let `autosave()` go back to unconditionally PATCHing `audit_ticked`/`sj.jobcard` — that unconditional write is the exact root cause here.
    - **ENQ2026072381434 remediation note**: the live DB was queried directly (not just code review) to find this — `audit_ticked` on that order is now stuck as `{draft:true, rooms:[...]}` with no `sign`, i.e. the original signed data is unrecoverable from the DB (it was overwritten before this fix shipped). The auditor's phone-local PDF (generated client-side before the server write) is the only surviving record of the original completion; the customer will need to re-sign to restore a valid digital job card.

65. **Job Overview date range + CSV export, Analytics metric drill-down + CSV export** (`Admin.html`, added 2026-07-25): three related self-serve reporting asks, all in Admin Job Overview / Analytics.
    - **Job Overview date range**: `jobsDateFilter` (single date) replaced with `jobsDateFrom`/`jobsDateTo`. `getFilteredJobs()` extracted as the single shared filter function (type/status/date-range/search) used by both `drawJobs()`'s table and the new CSV export — do not reintroduce a duplicate inline filter chain in only one of the two call sites, or they will drift.
    - **Job Overview CSV export**: `downloadJobsCSV()` exports whatever `getFilteredJobs()` currently returns (respects all active filters) as a UTF-8 CSV with a BOM (`﻿` prefix) for Excel compatibility. `_csvEscape()` is the shared quote/comma/newline escaper — reused by the Analytics drill-down export below. No new CDN library was added; XLSX was considered but CSV opens natively in Excel/Sheets without a new dependency.
    - **Analytics metric drill-down**: every `pctCard`/`ratingCard`/`npsCard` in `drawAnalytics` now optionally takes a trailing `drillKey`. `_anSetDrill(key,title,note,entityType,rows)` registers the underlying row list (one row per audit/subjob, each with a `result` string like `"✓ Completed"` / `"✗ Late"` / `"— No arrival log matched (excluded from %)"`) right where that metric is already computed — reusing `aFiltered`/`iCanon`/`aArr`/`iArr`/`aRatingMap`/`iRatingMap` etc., no new queries needed for the core numbers. `window._anDrill` is reset at the top of every `drawAnalytics(m)` call. Clicking a card calls `openAnalyticsDrill(key)`, which shows the row list in a modal and lazily fetches `customer_name` (not present in the lightweight Analytics queries) by PI from `audit_orders`/`install_orders` on first open only, caching it on the row objects (`d._resolved`) so re-opening the same metric doesn't re-fetch. `downloadDrillCSV(key)` exports the same rows.
    - `_anInstallArrivalStats`/`_anAuditArrivalStats` were extended to also return a `tagged` array (`{row/o, bucket}` per input row, including rows with `bucket:null` for "reached site but no arrival log matched") alongside the existing `agg`/`byName` — this is what feeds the Arrival On Time % drill-down. Do not remove `tagged`; it's the only place per-row arrival bucketing exists (the aggregates alone can't reconstruct which specific orders were late).
    - Do not add a drill-down that requires a new heavyweight query on every `drawAnalytics` render — the pattern is deliberately "compute from data already fetched for the aggregate numbers, lazy-fetch only the display-only `customer_name` on click."

66. **Permanent completed-job-card history for site audits (`audit_ticked_history`)** (added 2026-07-25, follows directly from note 64): note 64's fix stops a signed job card from being silently overwritten, but the live `audit_ticked` field is still just one mutable slot — if an auditor legitimately redoes and re-signs after a status revert, the *original* completed submission is gone from the live field with no trace, even though nothing was lost by accident this time. The user asked for a durable log specifically for the "completed, then status changed" case. New jsonb array column `audit_ticked_history` on `audit_orders` (SQL above) — never touched by the normal completion/autosave paths, only ever appended to.
    - **`_archiveAuditTicked(orderId, ticked, reason)`** (`Site_Auditor_App.html`): read-modify-write append, deduped by `sign.img` (so re-running the same archive call twice for the same signature is a no-op) — best-effort, wrapped in try/catch, must never throw or block the caller. Each entry is the full signed `ticked` object (`auditor, date, sign, rooms`) plus `archivedAt` (ISO timestamp) and `archivedReason`.
    - **Three archive points**, all calling the same helper (redundant on purpose — any one of them alone would miss some cases):
      1. `finishAudit`'s completion handler — archives every fresh signature immediately at completion time (`reason:'completed'`). This is the primary path going forward.
      2. `_retryPendingCompletions()` — archives after a previously-failed `audit_ticked` write successfully retries (`reason:'completed'`), so a network-hiccup completion isn't missed.
      3. `openJobCard()`'s signed-lock branch (note 64) — archives on every reopen of an already-signed card (`reason:'backfill-on-reopen'`), which backfills history for any order completed *before* this column existed, or whose archive write at completion time itself failed. The dedupe-by-`sign.img` check means this doesn't create duplicate entries on repeated reopens of the same still-unmodified signature.
    - **Fourth archive point, `SM_Audit_Dashboard.html`**: the backward-status-change confirm added in note 65 (moving a completed audit off `completed`) now also archives the current signed `audit_ticked` at that exact moment (`reason:'status-changed-to-<st>'`), inline (no shared helper — different file). This covers orders that get moved backward but whose auditor never reopens the job card again, which the field-app-side archive points alone wouldn't catch.
    - **Display**: `Admin.html`'s `openJobDetail(pi,'audit')` reads `o.audit_ticked_history` (already included via `select=*`, no query change needed) and renders a "📜 Completed Job Card History" section, most-recent-first, each row with a "📥 PDF" button (`downloadHistoricalPDF(idx)` → `genAuditPDF(order, history[idx])` — `genAuditPDF` only reads `order` + the passed `ticked` object, never `order.audit_ticked` directly, so passing a historical snapshot instead of the live one works with zero changes to the PDF generator). When the live card is currently a draft AND history exists, the draft-warning banner adds a pointer to the history section below it. `window._jdAuditHistory` holds the array for the click handler — historical entries are referenced by array index, never serialized into an inline `onclick` attribute (would be enormous and escaping-fragile given embedded base64 photos/signatures).
    - Install orders are NOT covered by this — the ask was specifically about site audits. The same pattern (`sj.jobcard` → `subjobs_jobcard_history` per subjob) could be added the same way if requested later.
    - Do not let any of the four archive call sites block or throw into their caller's real write path — history is strictly best-effort. Do not remove the `sign.img` dedupe check — without it, every reopen of an unmodified signed card (which now happens more often since note 64 makes reopening safe) would append a redundant duplicate entry.

67. **Site Audit → Order Conversion % and Material Depot Audit % were silently under-counting because of an incorrect "phone only populated from 1 Jul 2026" assumption** (`Admin.html`, fixed 2026-07-25): user reported a specific false negative — customer "Anusha" placed a site audit (`ENQ2026062676594`, created 2026-06-28) and a real install order (`ENQ2026063077218`, created 2026-06-30) with the same phone, but the Conversion % drill-down showed her as "Not converted yet". Root-caused to `renderAnalytics`'s `installLogRes` query: `install_orders?select=pi,phone,log&created_at=gte.2026-07-01` — `phone` was being merged into the analytics dataset from the SAME date-restricted query as `log`, under the assumption (previously stated in this doc, in the Analytics footer, and in the two affected pctCard notes) that install order phone numbers only existed from 1 Jul 2026 onward. **That assumption was never true** — live-queried all 190 non-deleted install orders and every single one has a non-null `phone`, including ones from 2026-06-11 (`phone` is a plain always-populated text column). The date restriction was actually needed for a completely different reason: `log` can embed multi-MB base64 photos on pre-storage-migration orders, which causes a Postgres statement timeout on a full-table fetch (confirmed live — `select=pi,phone,log` across all orders times out; `select=pi,phone` alone across all orders returns in ~0.3s). The two concerns had been incorrectly bundled into one query with one date filter.
    - **Fix**: `phone` (and, per note 68, `bm`/`customer_name`) is now selected directly in the main `install_orders_slim` query in `renderAnalytics` — no date restriction, no separate query — since `install_orders_slim` already exposes these as plain columns (`docs/supabase_slim_views.sql`). Only `log` (fetched via its own `installLogRes` query) keeps the `created_at=gte.2026-07-01` restriction, since that one genuinely still needs it. This fixes the phone-match denominator/numerator for BOTH `Material Depot Audit %` (install→audit phone match) and `Site Audit → Order Conversion %` (audit→install phone match) for every install order regardless of when it was created.
    - Verified live: re-ran the Conversion % drill-down for a wide date range — Anusha's row now correctly shows "✓ Converted to install order"; the overall converted count jumped from an undercount to the true figure across the full order history.
    - Removed the now-false "only install orders from 1 Jul 2026 onward have phone data" notes from both pctCard sub-labels and the Analytics footer — do not reintroduce that claim, and do not re-merge `phone` back into the same query as `log`. If `log`'s timeout risk ever needs revisiting, keep it in its own date-or-column-scoped query, separate from any field (like `phone`) that has no such risk.

68. **Analytics drill-down rows show Customer/Phone/BM, not just Enquiry ID/Date** (`Admin.html`, added 2026-07-25, follows note 65/67): user asked for more identifying detail in the drill-down tables (screenshot request: "store name, customer number"). `bm` and `customer_name` were added to the same two base queries in `renderAnalytics` that note 67 fixed for `phone` (`install_orders_slim?select=...,bm,customer_name,phone` and `audit_orders?select=...,bm,customer_name`) — all three are plain short text columns, no timeout risk, no reason they were ever left out of the base fetch other than not being needed yet.
    - `_aRow(o)`/`_iRow(o)` helpers (right before the `_anSetDrill(...)` calls) extract `{customer, phone, bm}` from an audit order / install order object respectively — every one of the 9 `_anSetDrill(...)` row-builders spreads one of these into each row now.
    - This let the previous lazy on-click `customer_name` fetch in `openAnalyticsDrill` (a `sbGet('audit_orders?pi=in.(...)&select=pi,customer_name')`/`install_orders` lookup, cached via a `_resolved` flag) be deleted entirely — `openAnalyticsDrill` is synchronous now, no loading spinner before the table renders, no per-click network round-trip. Do not reintroduce that lazy-fetch pattern; the data is already present on every row at registration time.
    - Modal table and `downloadDrillCSV` both gained two columns (Phone, BM) between Customer and Date. Phone renders as a `tel:` link in the modal.
    - **"BM" is the field label used throughout this app already** (Job Overview job detail modal, SM dashboards) — it is NOT necessarily a physical store name; it's whatever text was entered in that order's `bm` field at creation (often a salesperson/account name). Flagged here because at least one live order was observed with `bm` equal to the customer's own name (likely a data-entry mistake at order creation, unrelated to this app) — do not assume `bm` is a clean, validated store/branch identifier when building on top of it.

69. **Analytics tab now auto-refreshes every 60s (`Admin.html`, added 2026-07-27)**: user asked that metrics like Site Audit → Order Conversion % actually update if checked again days later, once some of the audits in a past range have since converted to install orders. The underlying phone-match logic already supported this (note 58/67 — it matches against the full all-time `installs` array, not date-restricted), and re-entering the Analytics view already re-fetched fresh data via `renderAnalytics`. The gap was that nothing refreshed while the admin stayed parked on the tab.
    - `renderAnalytics`'s data-fetch body (the `install_orders_slim`/`audit_orders`/`ratings`/`delivMeta`/`installLogRes` queries) was extracted, unchanged, into a new `_anFetchData()` — returns `true`/`false`, never touches the DOM, sets `window._anLastFetch=Date.now()` on success. `renderAnalytics(m)` now just shows the spinner, calls `_anFetchData()`, and on success calls `drawAnalytics(m)` — identical behavior to before the refactor.
    - New `_anPollTid` (module `let`, alongside `analyticsFrom`/`analyticsTo`): a `setInterval(...,60000)` started at the end of `renderAnalytics`, mirroring the existing Job Overview `_jobsPollTid` pattern (note 45) — guarded by `currentView==='analytics'&&!document.hidden`, and cleared in `navigate()` whenever the view changes away from `'analytics'`. Each tick calls `_anFetchData()` silently (no spinner — `drawAnalytics(m)` only runs if the fetch actually succeeded) so a background failure just leaves the last-good render on screen instead of showing an error mid-read.
    - **Focus guard**: if `document.activeElement` is `#anFrom` or `#anTo` (the custom date-range inputs) when a tick fires, that tick is skipped entirely — otherwise a background refresh would silently overwrite whatever date the admin was mid-typing into those two inputs before clicking Apply.
    - **Visible indicator**: a small `"🔄 Auto-refreshing every 60s · Last updated H:MM:SS AM/PM"` line was added directly under the date-range row so it's not a silent background behavior — reads `window._anLastFetch`.
    - 60s (not 30s like Job Overview) was chosen deliberately — this tab's fetch is heavier (3 parallel queries + 2 follow-up queries vs. Job Overview's simpler poll), and analytics numbers don't need sub-minute freshness.
    - Do not remove the focus guard or the `currentView==='analytics'` checks — without them either an in-progress date edit gets clobbered, or the poll keeps hitting Supabase after the admin has navigated to a different tab.

70. **"Pre-booked (Store)" tab in `SM_Audit_Dashboard.html` now shows fulfilled pre-bookings too, not just open ones** (2026-07-27): the `slot_converted` status and "✓ Service Created" button (`markSvcCreated`) already existed — clicking it flips a pre-booking's own placeholder row from `slot_reserved` → `slot_converted` once the SM has created the real Kylas service order for it, so it stops occupying/double-counting the slot. But the "Pre-booked (Store)" filter tab only ever matched `status==='slot_reserved'` exactly, and no other tab included `slot_converted` either — so the moment a pre-booking was marked fulfilled, it became **completely invisible** in the UI (findable only via a raw DB query), even though the row itself still existed. A live check while building this found **76 real fulfilled pre-bookings already silently orphaned this way**. User asked that fulfilled pre-bookings stay visible "in this tab" as a running record, while the reminder count at the top continues to only nag about the still-open ones.
    - **Fix**: the `slot_reserved` filter tab (`ordersView()`'s row filter) now matches `status==='slot_reserved'||status==='slot_converted'` — i.e. it's a permanent log of every store pre-booking, live and fulfilled together, not just a live queue.
    - **`slotConvertedDrawerBody(o)`** (new, mirrors `slotReservedDrawerBody`): read-only view of a fulfilled pre-booking — green "Fulfilled" banner (with who/when, read from the `"...pre-booking fulfilled"` log entry `markSvcCreated` already writes), the original pre-booking details/customer info, and a best-effort "Linked service order" note. The link is matched heuristically by `slot+date+phone-or-name` against the live `ORDERS` array — same signal already used by the auditor-assignment `linkedSvc` lookup elsewhere in this file — there is no stored foreign key between a pre-booking row and the real order it becomes. Clicking the linked-order note calls `openOrder(linkedSvc.pi)` directly (no need to close the drawer first — `openOrder` already fully re-populates title/body/foot for the new PI).
    - `drawerBody(o)`/`drawerFoot(o)` both gained a `status==='slot_converted'` branch (before their generic fallthroughs) — the footer is **Close only, no Delete**, deliberately, since the entire point of this fix is that fulfilled pre-bookings persist as a record; don't add a delete option back for `slot_converted` rows without discussing whether that undermines the record-keeping intent.
    - `openOrder()`'s subtitle logic (`_store`) was extended from `o.status==='slot_reserved'` to also include `'slot_converted'`, so the fulfilled drawer's subtitle correctly reads "Store {name}" instead of falling back to "BM {o.bm}".
    - A new note (`prebookTabNote`, shown only when `filterStatus==='slot_reserved'`) states the pending/fulfilled counts inline: `"N pending ... and M fulfilled"` — reuses the same `c` status-count object the stat tiles already compute, no new query.
    - The top **"Store pre-bookings" stat tile** (`c.slot_reserved`) and the **today's pre-booking banner** (`todaySlotRes`, filtered strictly on `status==='slot_reserved'`) were deliberately left untouched — they're meant to be the "keeps reminding us" nudge about what's still outstanding, and must not include already-fulfilled rows.
    - Do not extend the `slot_reserved`-tab special-case matching to any other filter tab (e.g. "all") — the fulfilled rows are meant to surface in exactly one place (this dedicated tab), not bleed into the main order list/counts.

71. **Mixed install orders (flooring + wallpaper in one enquiry) now track delivery/production/scheduling as fully independent per-category sub-jobs** (`SM_Install_Dashboard.html`, added 2026-07-27, prompted by ENQ2026072782038): `subjobs[].assignments[]` already let flooring and wallpaper have independent installers and installation dates — that part was already correct. The gap was that delivery date, delivery-confirmation status (pending/on-time/delayed + its ops-call reminder), and the custom-wallpaper production tracker were all still single fields on the *parent* `install_orders` row — one shared value for the whole order, so two categories delivering from different vendors on different dates had no way to be tracked separately. A live DB check found 4 of 205 live install orders already mixing flooring+wallpaper (ENQ2026062776747, ENQ2026062876897, ENQ2026062876892, ENQ2026071279303) before this shipped.
    - **Scope, deliberately narrow**: only orders with **both** a flooring and a wallpaper sub-job (`isMixedOrder(o)`) fork into independent tracks. Single-category orders (~98% of live orders) are completely untouched — verified live (a real single-category order rendered with zero mixed-only elements present).
    - **New per-sub-job fields** (mixed orders only, living inside the existing `subjobs` jsonb column — no schema/view migration needed): `deliveryDate`, `originalDeliveryDate`, `customWp`/`customWpStage`/`customWpMeta` (wallpaper only). `sj.status` reuses the *existing* order-level `STATUS` enum as-is, now including the delivery-confirmation states (`pending`/`deliv_ontime`/`deliv_delayed`/`call_na`) on a sub-job for the first time — previously those states only ever existed at order level, before subjobs were created.
    - **Fallback accessors** `sjDeliveryDate`/`sjOriginalDeliveryDate`/`sjCustomWp`/`sjCustomWpStage`/`sjCustomWpMeta` (each: sub-job's own field if set, else the order-level field) — this is what lets the 4 pre-existing legacy mixed orders keep rendering correctly with zero backfill migration; verified live, read-only, all 4 render through the fallback with no errors.
    - **Two fork points**, not one — found by review that an order can become mixed either at creation or later via editing:
      - `submitAddOrder()`: if the SKUs entered include both a flooring and a wallpaper type, both sub-jobs are created immediately at `status:'pending'` (skipping the shared pending→deliv_ontime→"Create service" gate single-category orders still use), seeded from the one delivery-date/custom-wp-checkbox already on the Add Order form — no form UI change.
      - `saveService()`: its existing `else if(fl.length&&!flSj)o.subjobs.push(...)` / wallpaper equivalent (used when an SM edits an *already-created* order's SKU list) now seed the *newly-added* category at `status:'pending'` — its own fresh delivery-confirmation cycle, since nobody has actually confirmed that category's delivery yet — **only** when this results in both categories being present (`bothPresent=fl.length>0&&wp.length>0`); the pre-existing category/sub-job is left completely untouched. Verified live: flooring already `assigned` (with its installer assignment intact) + newly-added wallpaper correctly seeded at `pending` with its own delivery date, in the same save.
      - Pending POs import (`usePORow`) and rectifications (`openRect`) were checked and confirmed to need no special handling — the former just pre-fills the same Add Order form (already covered), the latter is gated on `o.status==='completed'`, which the rollup below only reaches once every sub-job individually completes regardless of when each mixed track finished.
    - **Rollup — `syncParent(o)` extended, not a parallel function** (counts/table/filters/sort already all just read `o.status`): a real bug was caught and fixed here during testing — the *existing* post-creation rungs (e.g. `some sub-job is "assigned"`) would fire even while a *different* sub-job was still in a delivery-phase state, silently masking a real divergence (confirmed live: flooring `assigned` + wallpaper `pending` incorrectly rolled up to `assigned` until fixed). Fixed by checking delivery-phase agreement/divergence **first**, before any of the post-creation rungs: if any sub-job is in `DELIVERY_PHASE_STATUSES` (`pending`/`deliv_ontime`/`deliv_delayed`/`call_na`), roll up to the shared value only if every sub-job agrees, else `"mixed"` — return immediately, skipping the rest of the ladder. The post-creation ladder is otherwise unchanged, with a new trailing safety-net (`else o.status="mixed"`) replacing what used to be a silent no-op on an unmatched combination. Verified live end-to-end: diverge → "Mixed" chip appears on the main table; converge both sub-jobs back to the same status → chip correctly reverts (not stuck on "mixed").
    - **New `STATUS.mixed`** (`"Mixed — see details"`, `.c-mixed` navy chip) — synthetic, order-level-only, excluded from the manual "Set status" override menu (`Object.keys(STATUS).filter(k=>k!=='mixed')`) since it's a computed rollup, never a settable state.
    - **Per-sub-job UI**: `drawerBody(o)` skips the order-level "Custom Wallpaper Journey" / "Delivery — confirm with Operations first" / "Set status" sections entirely for mixed orders (replaced with a one-line banner), and instead renders `sjDeliveryBlock(o,sj)` (+ `sjCustomWpProductionSection(o,sj)` for a `customWp` wallpaper sub-job) inside each sub-job's own `.sjcard`, with its own "Delivery delayed/on time"/"Further delayed" actions and a "Confirm ready to schedule" action (the per-sub-job equivalent of the order-level "Create service" step) once that category's delivery is on time. `sjCwpBackHalfDone`/`sjCwpTrackHtml`/`sjCustomWpProductionSection` are **deliberately separate functions** mirroring `cwpBackHalfDone`/`cwpTrackHtml`/`customWpProductionSection` rather than parameterizing the originals — keeps the single-category path (98% of orders) completely untouched by this feature. A sub-job still in a delivery-phase status doesn't render its `data-assignsection` div at all (`DELIVERY_PHASE_STATUSES.includes(sj.status)`) — prevents showing a premature, empty installer-assignment section before that category's delivery is even confirmed.
    - **`drawerFoot(o)`**: mixed orders get an explicit branch (checked *before* the status-keyed branches) returning Close+Delete only — real actions live in each sub-job card now. This matters because a mixed order's rollup status can coincidentally equal `'pending'`/`'deliv_ontime'`/`'deliv_delayed'` (when both sub-jobs happen to briefly agree) — without the explicit branch, the order-level delivery buttons would appear and, if clicked, write to `o.status`/`o.deliveryDate` directly while leaving the real per-sub-job data untouched.
    - **`opsCallDue(o)`** kept as today's exact logic for single-category orders; for mixed orders it becomes `o.subjobs.some(sj=>subjobOpsCallDue(o,sj))` — true if *any* sub-job individually needs a call. This is enough to keep `needActionCount()`/stat tiles/list filters/the amber ops banner accurate with no changes to their own logic. `needActionView()`'s "Operations calls due" table was changed to a flattened `{o,sj}` list (mirroring the existing `reschedItems` pattern) so a mixed order contributes one row *per due sub-job*, each tagged with a category badge and its own delivery date — not one ambiguous order-level row.
    - **Main Orders table**: added a "Mixed" filter tab; the delivery-date column shows the earlier of the two sub-jobs' dates with a "2 dates — see details" hint for mixed rows (`mixedEarliestDelivery(o)`), also used for the delivery-date sort comparator.
    - **Known follow-up, not done here**: `Admin.html` analytics (the Delivery Date Tracking card, and the read-only custom-wallpaper stepper in `openJobDetail`'s job detail modal) still reads the order-level `delivery_date`/`custom_wp`/`custom_wp_stage` fields — for the handful of mixed orders these will show the legacy/fallback value rather than a true per-category breakdown. Only affects 4-5 orders today; flagged here rather than silently left unfixed.
    - Do not parameterize the original `cwpBackHalfDone`/`cwpTrackHtml`/`customWpProductionSection`/order-level delivery section to take an optional `sj` — that was deliberately avoided in favor of separate `sj*`-prefixed functions specifically so nothing about the single-category path could regress. Do not remove the delivery-phase-first check at the top of `syncParent` — that exact ordering is what fixes the "assigned masks a still-pending sibling" bug found live during testing.

72. **Install job-card permanent history + signatures moved to Storage** (added 2026-07-28, follows directly from note 64/66): a user report of missing photos on completed job cards ("ENQ2026071880436 shows job card missing") led to a live-data audit that found the note 64/66 corruption pattern was far more widespread than previously known — **66 of 145 (45%) all-time completed site audits** and **19 of 128 (15%) completed/partial install sub-jobs** currently have a stripped `{draft:true, rooms:[...no photos...], no sign}` or empty jobcard, all dated before the note 64 fix shipped (2026-07-25) and zero since — confirming the fix works but leaving the historical damage unrecoverable (no archive existed before 2026-07-25 to have captured the originals). Two follow-up hardening changes shipped from this investigation:
    - **`jobcard_history` on install sub-jobs** — the audit side already got a permanent, tamper-proof archive (`audit_ticked_history`, note 66); install never did. Rather than a new DB column, `jobcard_history` is a plain array field living directly on the subjob object inside the existing `subjobs` jsonb column (no schema/view migration needed — `install_orders_slim` doesn't need to expose it since only Admin's on-demand `select=*` job detail fetch reads it). New `_archiveInstallJobcard(orderId,sjId,jobcard,reason)` — duplicated verbatim in `Site_Installer_App.html` and `SM_Install_Dashboard.html` (no shared JS between files in this app) — mirrors `_archiveAuditTicked` exactly: its own independent read-modify-write of `subjobs` (never trusts a possibly-stale copy held elsewhere), deduped by `sign.img`, best-effort try/catch that must never block the caller's real write.
      - **Three call sites**, mirroring the audit side's four minus the one that doesn't apply (install has no `_retryPendingCompletions`-equivalent localStorage retry queue — out of scope here, not built as part of this change): (1) `Site_Installer_App.html`'s `reallyDone` (`#reallyDone` click handler), archived right after the completion `sbPatchLong` succeeds, `reason:'completed'`; (2) `Site_Installer_App.html`'s `openJobCard` signed-lock branch (note 64's `jcHadSign` guard), `reason:'backfill-on-reopen'` — backfills history for any sub-job completed before this column existed, or whose archive at completion time itself failed; (3) `SM_Install_Dashboard.html`'s `st==='created'` backward-revert cascade, at the exact moment a signed sub-job's status/assignments are reset (`!skipSigned` branch), `reason:'status-changed-to-created'` — mirrors the audit-side SM backward-move archive (note 65).
      - **Admin.html** (`openJobDetail`'s install branch): renders a "📜 Completed Job Card History" section per sub-job (mirrors the audit modal's section exactly), with a `downloadInstallHistoricalPDF(sjIdx,histIdx)` button per entry that calls the existing `genInstallPDF(order,sj,historicalJobcard,installerName)` unchanged — `genInstallPDF` only reads whatever jobcard object it's passed, never `sj.jobcard` directly, so a historical snapshot works with zero PDF-generator changes. Also fixed a related pre-existing gap found while building this: the install detail modal had no `isDraft` detection at all (unlike the audit modal, note 64/66) — a sub-job with a stripped `{draft:true, rooms:[...]}` jobcard rendered its rooms as if it were a normal, valid, completed card with no warning. Added the same `isDraft=jc&&jc.draft&&!hasSign` check the audit modal already had, now showing an amber "⚠️ Job card draft — client sign-off not yet completed" banner (plus a pointer to the history section when one exists) instead of silently presenting corrupted/incomplete data as legitimate.
    - **Signature images uploaded to Storage instead of embedded as base64** (`Site_Auditor_App.html`'s `#finishAudit` handler, `Site_Installer_App.html`'s `#reallyDone` handler): the client/customer signature canvas (`jcSignPad.export()`) previously went straight into `sign.img` as a base64 data URL embedded directly in the JSONB column — the exact pattern the user flagged from a live Supabase screenshot as suspicious. Now routed through the same `_uploadPhoto()` helper (same `job-photos` Storage bucket) already used for room photos, with the same fallback-on-failure pattern as `_resizeAndStore`: `let sigImg=jcSignPad.export();try{sigImg=await _uploadPhoto(sigImg);}catch(e){}` — if the upload fails, `sigImg` stays the base64 dataURL and everything works exactly as before. No changes were needed anywhere `sign.img` gets read: every PDF generator across all 5 files (`compressImg`/`compress`/`_compressAdmin`/`_compressSM`/`_compressInst`) already handles both base64 and `https://` URLs (`crossOrigin='anonymous'` + try/catch, note 30), and `<img src>` in the Admin modals is format-agnostic. This is a bandwidth/consistency improvement, not a data-loss fix on its own — it does not need a backfill of existing base64 signatures (same backward-compat stance as the 2026-07-01 room-photo migration, note "Photo Storage").
    - **Verification**: both changes were tested with full network mocking (no real Supabase reads or writes) — `_uploadPhoto` success returns a well-formed public Storage URL, failure correctly falls back to the original base64 string; `_archiveInstallJobcard` was tested via intercepted GET/PATCH confirming correct `jobcard_history` payload shape on first archive and a true no-op (no PATCH fired) on a repeated call with an identical `sign.img`; Admin.html's history section, draft-warning banner, and `downloadInstallHistoricalPDF` wiring were verified against a fully synthetic mocked order (real DB never touched).
    - **Known gap, not built here**: no `_retryPendingCompletions`-equivalent exists on the install side at all (audit has one, for retrying a failed completion write via a localStorage queue) — a network failure at `reallyDone`'s completion write currently just shows an error toast with no automatic retry-on-next-load. Out of scope for this change; flagged for whoever picks up install/audit parity work next.
    - Do not remove the `isDraft`/dedupe-by-`sign.img` checks, and do not let `_archiveInstallJobcard`'s read-modify-write collapse into reusing whatever `subjobs` array another function already has in memory — the whole point of its own independent fetch is to avoid clobbering concurrent changes to sibling sub-jobs from other installers on the same order.

73. **Service Managers can now see the Analytics tab — by reusing `Admin.html`'s tab directly, not duplicating it** (added 2026-07-28): user asked for the Analytics section to be visible from "the site audit and site installation app" (`SM_Audit_Dashboard.html` / `SM_Install_Dashboard.html`). Given how many bugs in this codebase have come from the SAME analytics logic existing in more than one place (note 58's five-disconnected-scopes rewrite, note 57's duplicated-boolean-under-two-labels), a third/fourth copy of `drawAnalytics` + its ~15 calc helpers was deliberately avoided in favor of granting scoped access to the one real copy.
    - **`Admin.html`'s session guard** now accepts `role==='service_mgr'` in addition to `'admin'` (was a hard `SESSION.role!=='admin'` redirect-to-login before this). When the role is `service_mgr`, `document.body` gets class `sm-analytics-only`, which a single CSS rule (`body.sm-analytics-only .admin-only{display:none}`) uses to hide every nav item/section EXCEPT Analytics — Overview, the whole "Dashboard"/"Manage" rail sections, Role Viewer, Job Overview, Performance, and Live are all tagged `.admin-only` in the rail HTML. `navigate('analytics')` is called directly instead of `navigate('overview')` for this role, so a service_mgr session lands straight on Analytics with no extra click. The header badge (`#hdr-badge`) is relabelled `ANALYTICS` (was `ADMIN`) and the subtitle (`#hdr-subtitle`) becomes `Analytics` (was `Admin Console`) for this role only — both ids were added to elements that previously had no id.
    - **Both SM dashboards** gained a new "INSIGHTS" rail group with a single `<a href="Admin.html">📉 Analytics</a>` link (real `href`, not a `data-view`-driven SPA tab) — clicking it does a normal page navigation to `Admin.html`, which re-reads the same `md_user` localStorage session and renders the scoped view above. The existing generic `#rail a` click-handler loop (`activeView=a.dataset.view;...;render()`) in both dashboards was changed to `if(a.hasAttribute("href"))return;` before attaching its `onclick`, so this new anchor navigates natively instead of being hijacked into the SPA router with an undefined view.
    - This is UI-level scoping only, same as every other role gate in this app (no RLS, anon key has full read/write) — a service_mgr session hitting Admin.html directly still only gets the Analytics view because of the client-side render logic, not a server-side permission boundary. Consistent with how every other role check in this codebase already works.
    - Verified with Playwright against a fake `md_user` localStorage session (role `service_mgr`) served from a local static server — confirmed the Analytics link appears in both SM dashboards' rails, clicking it lands on a scoped Admin.html showing ONLY the Analytics nav item with the relabelled badge/subtitle, and that logging in as `admin` still shows all 7 nav items and lands on Overview as before (no regression). The scoped view was also confirmed to render real production Analytics data correctly (read-only GET calls only — no writes were made during this test, consistent with this project's safe live-test convention).
    - Do not duplicate `drawAnalytics`/its helpers into either SM dashboard file — if a future ask wants the SM Analytics view scoped differently from Admin's (e.g. audit-only data in `SM_Audit_Dashboard`'s link target), extend this same reused-tab pattern (e.g. a query param `Admin.html` reads to hide one of the two sections) rather than forking the calculation logic.

74. **Arrival On Time % denominator undercounted — excluded jobs currently `atsite` even though they already have real arrival data** (`Admin.html`, fixed 2026-07-28): user reported 9 site installation subjobs for the day but the Installer Arrival On Time % card only benchmarked against 5. Root cause: the "reached site" eligibility set fed into `_anInstallArrivalStats`/`_anAuditArrivalStats` (`iReached`/`aReached`) was filtered to `status` completed/partial (install) or completed-only (audit) — but an "arrived at site" log entry (the exact thing this metric matches against, see `_anArrivalBucket`) is written the moment a job's status flips to `atsite` (confirmed in both `Site_Installer_App.html` and `Site_Auditor_App.html`: `adv("atsite",...)` writes `"Installer/Auditor arrived at site"` to the log at that transition), so a job still `atsite` (arrived, not yet finished) already has valid arrival data sitting unused. In the reported example: 9 total subjobs = 6 completed + 3 atsite; the old filter only considered the 6 completed ones, and of those only 5 had a matching log entry — hence "1 of 5" instead of the true fuller set.
    - **Fix**: new `iArrivalEligible`/`aArrivalEligible` sets (`status` in `completed`/`partial`/`atsite` for install, `completed`/`atsite` for audit) feed `iReached`/`aReached` — kept deliberately separate from `iDone` (install) and the inline `aCompleted`/`aFiltered.filter(o=>o.status==='completed')` computations that drive **Completion %**, since an in-progress `atsite` job must NOT count as "done" for that metric. Only the arrival-tracking denominator was broadened.
    - This also automatically widens the per-installer/per-auditor table's on-time % column (via `iArr.byName`/`aArr.byName`) and the `iArrival`/`aArrival` drill-down row lists, since both are computed downstream of the same `iReached`/`aReached` sets — no separate fix needed there.
    - Do not re-narrow `iReached`/`aReached` back to completed-only, and do not merge `iArrivalEligible` back into `iDone` — they must stay separate: one answers "has this job actually arrived and is arrival-time measurable," the other answers "is this job fully completed."
    - Single fix location — this is the same `Admin.html` Analytics tab both SM dashboards link to per note 73, so no separate change was needed for the Service Manager view.
    - **Verified live-data effect** (read-only, via the app's own already-loaded `window._anData` in a browser session — not a separate direct DB query): over the default 7-day range this widened install `iReached` from 26→30 rows and picked up one additional genuine on-time/late data point (21→22 matched); the audit side happened to have zero currently-`atsite` orders in that same window so its numbers were unchanged there, but the fix is equally in effect for audit.
    - **Second, deeper finding from the same investigation**: in the exact single-day example the user reported, all 3 `atsite` subjobs turned out to have been pushed to `atsite` via the SM's manual `[data-setstatus]` override (`"Status set to At Site (manually)"` — same override family as note 55/71's "Set status" button), NOT via the installer's own field-app arrival confirmation. That log message doesn't match `_anArrivalBucket`'s `"arrived at site"` text, so these 3 rows are correctly *eligible* but still correctly *excluded from the %* (visible in the `iArrival` drill-down as "no arrival log matched") — because there genuinely is no real arrival timestamp to judge on-time/late against. This means an SM force-setting a job to At Site will always leave that job structurally unmeasurable for this metric, by design (fabricating an on-time/late verdict with no real signal would be worse than excluding it). **Now surfaced to the SM directly — see note 76.**

75. **Job Overview's 4 header stat tiles (Total Jobs / Active Now / Completed / Unassigned) are now clickable filters** (`Admin.html`, added 2026-07-29): user wanted clicking e.g. "63 Unassigned" to jump straight to the list of exactly those 63 jobs, instead of the numbers being decorative and requiring a manual pill/search hunt to find the matching rows.
    - **Two new filter values** recognized by `getFilteredJobs()`: `'active'` (`['assigned','onway','atsite','scheduled'].includes(j.status)` — the exact same set `drawJobs`'s `jc.active` tile count already used) and `'unassigned'` (`!j.assignee`). Both were also added to the existing filter-pill row (`['all','audit','install','active','unassigned','pending',...]`) so they're reachable/re-selectable without going back to the stat tile, and so the pill row's `.active` highlight stays in sync with a stat-tile click.
    - **New `setJobStatFilter(f)`** (distinct from the existing `setJobFilter(f)`): sets `jobsFilter` AND clears `jobsDateFrom`/`jobsDateTo`/`jobsSearch`. This is required because the header stat counts (`jc.total`/`jc.active`/`jc.done`/`jc.unassigned` in `drawJobs`) are always computed over the full unfiltered `realJobs` — if a date range or search was still active from a previous action, clicking "380 Total Jobs" without resetting those would show fewer than 380 rows, contradicting the number just clicked.
    - **CSS**: new `.stat-card.clickable`/`.stat-card.clickable.active` classes (hover border + navy-filled selected state, mirroring `.filt-btn.active`) — deliberately scoped as additive modifier classes, not changes to the base `.stat-card` rule, since that class is also used by non-clickable stat tiles elsewhere in this file (e.g. Overview page) that must not become clickable-looking by accident.
    - Verified live (read-only, real production data) via Playwright: all 4 tiles' row counts matched their header numbers exactly after clicking, the pill row highlighted correctly, and a pre-set date filter was correctly cleared by the click.

76. **SM "Set status → At Site" manual override now shows an informational warning (not a block)** (`SM_Audit_Dashboard.html` + `SM_Install_Dashboard.html`, added 2026-07-29, direct follow-up to note 74's second finding): user confirmed they want a warning here but explicitly do NOT want the action restricted — same soft-gate-not-hard-block philosophy as [[feedback_material_depot_ui_and_testing]]'s enforcement-philosophy entry.
    - `isManualAtSite = st==='atsite' && o.status!=='atsite'` computed before `o.status=st` overwrites it. When true: the log entry gets an extra annotation, `" · ⚠ no field-confirmed arrival (SM override — excluded from Arrival On Time % tracking)"`, appended alongside the existing `overrideNote` (same string-concatenation pattern, not a new log field), and the post-save `toast(...)` call shows `"⚠ Marked At Site manually — no arrival confirmed from the auditor/installer app, so this won't count in Arrival On Time % tracking"` instead of the normal `"Status: <label>"` toast.
    - **SUPERSEDED 2026-07-31 by note 85**: this entry originally said the At-Site manual override had *no* `confirm()`/`prompt()` and only annotated after the fact. As of note 85, EVERY manual status set (At-Site included) now requires a typed note via the shared `requireNote()` helper — the user explicitly asked that all manual SM status changes be gated by a mandatory logged note. The At-Site-specific analytics annotation (`· ⚠ no field-confirmed arrival (SM override — excluded from Arrival On Time % tracking)`) is still appended on top of the note. So the "informational, never gated" framing below is historical; the toast wording is unchanged. If you ever want to make a specific manual action informational-only again, it must be carved out of the `requireNote` call explicitly.
    - Verified via a synthetic in-memory order injected directly into `ORDERS` (no `id` field, so the `if(o.id)await sbPatch(...)` write is naturally skipped — no real DB write, per the safe-testing method) in both dashboards: the status change went through unrestricted, the log annotation and toast both appeared correctly, and a network-route interceptor confirmed zero non-GET requests were attempted.

77. **Analytics tab: Site Audit and Site Installation now have fully independent date ranges** (`Admin.html`, added 2026-07-29): previously one shared `analyticsFrom`/`analyticsTo` pair (with one date-range picker above both sections) drove both sections — user wanted to look at a different window for each (e.g. audits this week vs installs this month) without one section's range fighting the other's.
    - Split into `analyticsAuditFrom`/`analyticsAuditTo`/`analyticsInstallFrom`/`analyticsInstallTo` (four module `let`s, replacing the old two). Each defaults independently to "last 7 days" in `renderAnalytics` (same default as before, just computed twice).
    - New `_anDateRowHtml(kind)` helper (`kind` is `'Audit'` or `'Install'`) renders one Apply/Last-7/30/90-days control block, referencing `analytics${kind}From/To` and `an${kind}From/To` ids — used once inside each section's header (`${_anDateRowHtml('Audit')}` right after the Site Audit section's hero stat, `${_anDateRowHtml('Install')}` likewise for Site Installation) instead of one shared block above both sections. Kept as one function instead of hand-writing two near-identical blocks so they can't drift out of sync.
    - `drawAnalytics(m)`'s top now reads `aFrom/aTo` (audit) and `iFrom/iTo` (install) instead of one shared `from/to` — `aFiltered` uses `aFrom/aTo`, `_anInstallCanon(installs,iFrom,iTo)` and the arrival `iTrackFrom`/`aTrackFrom` floor calculations use their own respective pair. `_anInstallCanon`'s own function signature still takes generic `from`/`to` parameters (unaffected — it's a pure function, just called with different args per section now).
    - The 60s auto-refresh's focus-guard (which skips a silent background refresh while the admin is mid-edit in a date input) now checks all 4 new ids (`anAuditFrom`/`anAuditTo`/`anInstallFrom`/`anInstallTo`), not just the old 2.
    - Verified live (real production data): narrowing the Site Audit range to a single day changed only the Site Audit section's total, while the Site Installation section's total and its own date inputs stayed completely untouched.

78. **Investigated why Installer Arrival On Time % still undercounts after note 74's fix — root-caused to real missing/broken data, not a logic bug** (`Admin.html`/SM dashboards, investigated 2026-07-29): user reported 35 total install subjobs in a 7-day range, only 22 had measurable arrival data (denominator), and pushed back that anything not `reschedule` should be counted. Pulled the exact 8 unmatched rows via the app's own already-loaded `window._anData` (read-only, no direct DB query) and found three distinct, real causes — the matching logic itself (note 58's `_anArrivalBucket`/`_anInstallArrivalStats`, name+date match against the row's *currently* assigned installer(s)) was behaving exactly as designed in every case:
    - **4 of 8 — SM manually forced status straight to `atsite` or `completed`, skipping the installer's own field-app arrival confirmation entirely** (e.g. `ENQ2026072581767`: installer logged "on the way" via the app, then an SM used the "Set status → At Site" override instead of waiting for the installer's own arrival confirmation, then the installer's own app auto-completed the job normally afterward — so a real installer really did the work, but no arrival timestamp was ever captured). One of these (`ENQ2026062376061-R`, a rectification order) skipped `atsite` entirely — SM force-set it directly from `assigned` to `completed` with an explicit override reason ("Ashish confirmed") — note 55's existing "forced Completed without job card" guard fired correctly for this, but note 76's At-Site warning did not (it only checks `st==='atsite'`). **User decision (same session): do NOT extend note 76's warning to this path** — an SM override, whichever form it takes, should never be treated as real field-confirmed arrival data, and that's exactly what already happens (these stay correctly excluded from the %). No code change from this bullet.
    - **2 of 8 — orphaned arrival log from BEFORE an installer reassignment**: `ENQ2026072281071` and `ENQ2026070978801` both have a real, valid "arrived at site" log entry — but for an installer who was later swapped out, so the row's *currently*-assigned installer's name didn't match anyone in the log. **User decision: yes, credit former assignees — see note 79**, which reworks the metric to track every real logged arrival as its own attempt rather than only the current assignee's.
    - **2 of 8 — orders with a completely EMPTY `log` array** (`ENQ2026062576445`, `ENQ2026063077169` — plus `ENQ2026061875242` from note 74's original investigation, also empty): no "Order added," no assignment history, nothing at all, despite the order being `completed`/`partial` with real SKU/customer data. Not explainable by any code path in this app. **User decision: correctly excluded already — no change wanted.** Still worth the user checking directly in Supabase as a raw data anomaly, unrelated to this feature.
    - **How to apply**: do not "fix" this by loosening the arrival-eligible set further or by counting unmatched rows as failures by default — every unmatched row here has a genuine, identifiable reason rooted in real ops behavior or a data anomaly, not a computation bug.

79. **Arrival On Time % reworked to count every real logged arrival attempt, not just the subjob/audit's current assignee** (`Admin.html`, added 2026-07-29, direct implementation of note 78's second finding): user's explicit ask — punctuality should be tracked "for the number of times an auditor or installer goes to the client site," so if a job is reassigned mid-flight, the FIRST installer's real on-time/late arrival should still count for them, and the SECOND installer's own arrival (against their own assigned time) should count separately too. Explicitly NOT wanted: crediting an SM-override-induced status change as if it were a real arrival (see note 78's first bullet — deliberately left alone), and NOT wanted: counting the two empty-log anomaly orders (also deliberately left alone, already excluded).
    - **The core problem this solves**: `sj.assignments`/`o.auditor_name` are overwritten in place on every reassignment (`SM_Install_Dashboard.html`'s `saveAssign`: `sj.assignments=valid`; `SM_Audit_Dashboard.html` clears `auditor_id`/`auditor_name`/`auditor_email` on a reschedule rebook) — the live document only ever reflects the CURRENT assignee, so a past installer's real arrival log became permanently unmatchable once someone else took over. The only surviving record of "who was assigned when" is the log text itself.
    - **New `_anParseSlotTime12h(str)`**: recovers a 24h `"HH:MM"` from a log message's trailing 12h time (e.g. `"...· 2:00 PM"` → `"14:00"`) — necessary because `slotLabel()`/`fmtDate()` only ever write the display-formatted string into log text, never the raw value, so this is the only way to know what slot was in effect at a past point in an order's history.
    - **New `_anInstallOrderAttempts(order)`**: replays one install order's `log[]` chronologically (sorted by `d`), maintaining a tiny per-subjob-type state machine (`{names:Set, slot}`) updated by matching the exact, deterministic message formats this app's own code generates: `"${type} assigned: ..."` / `"${type} rescheduled...: ..."` (names, parsed as everything after the first `': '`) and `"${type} slot booked: ..."` / `"${type} slot rescheduled...: ..."` (slot, via `_anParseSlotTime12h`) and `"${type} slot cleared and installer released"` (resets both). On each `"...arrived at site"` entry, if `l.who` is in the CURRENTLY-tracked name set for a type (checked independently per type, so mixed orders don't cross-attribute), it's recorded as a real attempt — deduped by `(name, day)` so a network-retry re-firing the same confirm doesn't count as two visits (verified live: `ENQ2026070978801` had Raju Kumar's arrival logged twice 20 minutes apart, correctly collapsed to one).
    - **New `_anAuditOrderAttempts(order)`**: same idea, single-type (no per-type split) — `"Auditor assigned: NAME"` (name parsed up to the next ` · ` or ` ⚠`), `"Slot booked/updated"` / `"Date/time updated"` (slot), `"Slot rebooked — auditor cleared..."` (resets name AND updates slot in one branch, matching that this message genuinely does both in `SM_Audit_Dashboard.html`).
    - **`_anInstallArrivalStats(installs,from,to,reachedRows)` / `_anAuditArrivalStats(audits,from,to,reachedRows)` rewritten** to call the above per order across the FULL all-time array (not the pre-filtered canonical `iReached`/`aReached` used before) — an attempt's inclusion is scoped purely by its OWN arrival-log date falling in `[from,to]`, completely independent of the parent subjob/order's current status or date, since a real visit's punctuality doesn't stop being real just because the job was later reassigned or rescheduled. `reachedRows` (the existing note-74 eligible-and-reached set) is now used ONLY to emit a `bucket:null` placeholder for jobs that reached site but got zero real matched attempts (e.g. the note-78 SM-override and empty-log cases) — still excluded from the %, still visible in the drill-down for transparency, exactly as before.
    - **`tagged` shape changed** from `{row,bucket}`/`{o,bucket}` to a flat `{pi,date,order,type,name,bucket}` (install) / `{pi,date,order,name,bucket}` (audit) — one entry per attempt, so a reassigned job can now appear more than once. Updated both `_anSetDrill('iArrival',...)`/`('aArrival',...)` call sites and their result strings to show the installer/auditor name per row (e.g. `"✓ On time (Amir Khan)"`).
    - **`_anInstallerMap`/`_anAuditorMap` extended** to also emit a row for any name present in `arrByName` but not already covered by a currently-assigned row (e.g. an installer reassigned off every live subjob) — with `orders:0`/`completed:0` but real `arrOnTime`/`arrLate` — so the per-person breakdown table can't silently disagree with the aggregate % above it. In practice this rarely produces a 0-orders row, since most reassigned-away installers still have other current work elsewhere in the same range (confirmed live: Amir Khan, reassigned off `ENQ2026072281071`, still shows 9 current orders from other jobs — his 38% on-time figure now correctly includes the extra attempt from the job he's no longer on).
    - **Verified live** (real production data, read-only): both previously-orphaned attempts (`ENQ2026072281071` → Amir Khan, `ENQ2026070978801` → Raju Kumar + Pintu Sharma, two separate installers on two separate dates for the same order) now show up correctly as independent attempts; the duplicate-arrival-log dedup was confirmed to collapse a same-day double-write to one; the drill-down modal, stat-card, and independent-date-range features from the same session were all re-verified unaffected (no regressions).
    - Do not revert to matching only against the row's *current* assignments — that was the entire point of this rework. Do not remove the `(name, day)` dedup — without it, a retried log write inflates the count. If a future SM-generated log message format changes (assignment/slot-booking text), the parsers here need updating to match, since they rely on exact string prefixes, not a structured field.

80. **Fixed a real bug in note 79's audit-side reconstruction, found by spot-checking the just-shipped rework** (`Admin.html`, fixed 2026-07-29, same day as note 79): user asked to also check the Auditor Arrival numbers after note 79 shipped. Spot-checked 4 real matched attempts against raw order logs — all correct, including a triple-fired "Auditor arrived at site" write 1 second apart (correctly deduped to one) and a genuine mid-flight auditor reassignment (correctly attributed only to the auditor who actually went, Anil Chakali, not the originally-assigned Sathish Jangam who never visited). But 2 of the excluded/unmatched audits (`ENQ2026070177346`, `ENQ2026070377746`) turned out to be wrongly excluded — both had a normal HH:MM slot and a real, valid "arrived at site" log entry, yet got no attempt recorded.
    - **Root cause**: `_anAuditOrderAttempts`'s original branch treated ANY log message starting with `"slot rebooked"` as the "auditor cleared, needs reassignment" signal (clearing the tracked name), based on `SM_Audit_Dashboard.html`'s current message text `"Slot rebooked — auditor cleared, reassignment needed: ..."`. But these two live orders carry an older-format log entry, `"Slot rebooked: Sat, 4 Jul · 9:00 AM — Client preference"` — no "auditor cleared" wording at all, from a prior revision of this app's reschedule-rebooking code. Their `auditor_name` was never actually disconnected (confirmed live) — the auditor stayed assigned the whole time and genuinely visited — but the parser wiped its tracked name anyway on seeing "slot rebooked", orphaning a perfectly real arrival right after.
    - **Fix**: check for the substring `"auditor cleared"` anywhere in the message (not a fixed prefix) to decide whether to clear the tracked auditor name; every `"slot booked"`/`"slot updated"`/`"slot rebooked"`/`"date/time updated"` message still updates the tracked slot regardless. This correctly handles both the current wording (clears + updates) and this older wording (updates only) without needing to hardcode a full string match per historical app revision.
    - **Verified live**: both previously-misexcluded audits now correctly show a matched attempt; the two genuinely-excluded audits remaining (`ENQ2026062876847`, `ENQ2026062776807`) use a legacy pre-HH:MM slot ID (`"sf1"`) with no parseable time in their log text at all — a real, expected gap (same class as note 74's `TRACK_FROM` floor), not a bug. Re-ran the full regression suite (stat cards, independent date ranges, install attempt reconstruction) — no regressions.
    - **How to apply**: this is a good example of why note 79's own closing caveat matters — these parsers rely on exact log-text substrings, and this app's log-message wording has changed across revisions at least once already for this exact code path. If arrival numbers look off again after a wording change elsewhere in this app, check for a similar "old message format the parser doesn't recognize" cause before assuming the underlying data itself is broken.

81. **Job Overview: Type (Audit/Install) and Status (stat-tile category) filters are now independent and combinable** (`Admin.html`, added 2026-07-29): user pointed at the "67 Unassigned" tile and asked that clicking Audit/Install narrow WITHIN that 67, not replace it — e.g. "how many of these 67 unassigned jobs are audits vs installs," not "show me all audits" (losing the Unassigned scope). Previously `jobsFilter` was a single mutually-exclusive value covering both dimensions (`'audit'`/`'install'` alongside `'unassigned'`/`'active'`/`'completed'`/etc.), so picking a type always discarded whatever status pill was selected.
    - **New `jobsType`** (`'all'|'audit'|'install'`), separate from the existing `jobsFilter` (now status-only: `'all'|'active'|'unassigned'|'pending'|'assigned'|'scheduled'|'onway'|'atsite'|'completed'|'reschedule'`). `getFilteredJobs()` applies both as independent AND-combined predicates (type first, then status), instead of one `if/else if` chain.
    - **New `setJobType(t)`** — mirrors `setJobFilter(f)` but only ever touches `jobsType`, leaving `jobsFilter`/date range/search untouched, so the two dimensions never clobber each other.
    - **Toolbar UI split into two pill groups** in the same row: `['all','audit','install']` (labelled "All Types"/"Audit"/"Install") followed by a thin divider, then the existing status pills minus `audit`/`install` (`['all','active','unassigned','pending','assigned','scheduled','onway','atsite','completed','reschedule']`, labelled "All Jobs"/"Active Now"/"Unassigned"/etc.). Both groups highlight independently based on `jobsType`/`jobsFilter` respectively.
    - **Stat-tile clicks (`setJobStatFilter`, note 75) now also reset `jobsType='all'`** — the 4 header numbers are always all-type totals (e.g. "67 Unassigned" = audits + installs combined), so jumping to one resets type back to "All Types" alongside the existing date/search reset, keeping the "number you clicked = rows you see" guarantee from note 75 intact. The 4 stat-card active-state checks were updated to require `jobsType==='all'` too, so a tile only shows as selected when its number is exactly what's on screen.
    - **Verified live** (real production data): clicking the Unassigned tile (67) → Audit type pill shows exactly the unassigned audits (10) with the Unassigned status pill still highlighted; switching to Install type while still on Unassigned correctly shows 57 rows (10+57=67, confirming the split adds up); clicking Total Jobs afterward resets both dimensions back to "All Types"/"All Jobs" showing all 388.

82. **Install Orders header stat tiles fixed to sum to the true total; several statuses that had NO tile or filter tab at all are now surfaced** (`SM_Install_Dashboard.html`, fixed 2026-07-30): user noticed the 5 header tiles (Installation orders / Ops calls due today / Delivery delayed / Scheduled+in progress / Completed) didn't add up — 223 total but only 171 accounted for (`deliv_delayed` 11 + `scheduled+assigned+partial` 16 + `completed` 144). Root-caused via a live read-only pull of the full `ORDERS` array (`window.ORDERS` in a browser session, not a raw DB query — see the established safe-testing workaround in [[feedback_material_depot_ui_and_testing]]): the missing 52 were real orders sitting in `pending` (22), `created` (15), `call_na` (5), `deliv_ontime` (4), `reschedule` (3, already counted separately in the sidebar's own "To reschedule" nav but absent from these tiles), `atsite` (2), and `callpending` (1) — none of which had a header tile, and several of which (`deliv_ontime`, `call_na`, `callpending`, `atsite`, `onway`) had **no filter tab at all** in the Orders view, meaning the only way to find one was scrolling "All" and reading status chips by eye. A second, smaller bug was found in the same investigation: the old "Scheduled / in progress" tile's formula (`c.scheduled+c.assigned+c.partial`) silently excluded `onway`/`atsite` even though its own `filterStatus==="live"` click-through filter already included them — so clicking that tile could show more rows than the number printed on it.
    - **Fix**: replaced the 5 tiles with 6 mutually-exclusive lifecycle buckets that always sum to `ORDERS.length`: **Awaiting delivery confirmation** (`pending`+`call_na`, key `await_confirm`), **Delivery delayed** (`deliv_delayed`, unchanged), **Confirmed — awaiting scheduling** (`deliv_ontime`+`created`, key `await_schedule`), **Scheduled / in progress** (`scheduled`+`assigned`+`callpending`+`onway`+`atsite`+`partial`, key `live` — now actually matches its own click-through filter), **To reschedule** (`reschedule`, newly given a tile), **Completed** (`completed` only, `partial` moved out to the in-progress bucket where it semantically belongs). "Ops calls due today" was deliberately dropped as a tile (it's a date-driven cross-section of `pending`/`deliv_delayed`/`call_na` — not a distinct status — so including it in a sum-to-total row would double-count) but is kept fully visible via the existing amber "Review calls" banner + its own `calls` sidebar nav tab, which already showed the identical number.
    - **Filter tabs extended**: the Orders view's tab row gained `call_na`, `deliv_ontime`, `callpending`, `onway`, `atsite`, and `reschedule` — every status in `STATUS` is now individually filterable from this view, not just the ones that happened to have a tile before.
    - **Verified live** (read-only browser session, no writes): the 6 tiles summed exactly to the total; clicking each tile's row count matched its printed number exactly (e.g. `live` 18/18, `await_confirm` 27/27); the 3 newly-added individual filter tabs rendered with no errors.
    - Do not reintroduce a stat tile/sum that mixes a real order status with a date-derived cross-section (like ops-calls-due or today's-installs) in the same summable row — that's the specific mistake this fix corrects. If a new status is ever added to `STATUS`, make sure it's folded into exactly one of the 6 buckets above (or given its own), not silently left out.

83. **Store Team App — added HSR Layout as a 4th experience centre** (`Store_Team_App.html`, added 2026-07-30): `STORES` array (previously `['JP Nagar','Whitefield','Yelahanka']`) is the single source of truth for the store picker overlay, the day-view store selection, and the pre-booking PI-code prefix (`storeCode` is derived dynamically from the store name string, `store.replace(/\s+/g,'').toUpperCase().slice(0,6)` → `'HSRLAY'` for HSR Layout, no collision with the other 3 codes) — confirmed via a repo-wide grep that no other file (`Admin.html`, either SM dashboard) hardcodes a store list; the `bm` field used elsewhere in this app is free text, not constrained to `STORES` (see note 68). Adding the array entry was therefore sufficient on its own — no CSS/layout change needed either, since `.store-option` renders as a stacked list, not a fixed-column grid.
    - **Verified live** (read-only Playwright, no bookings written): all 4 stores render in the picker, selecting HSR Layout updates the header and loads the day view with no console errors, and reopening the picker correctly shows it as the active selection.

84. **Offline-resilient status/job-card timestamps — field workers no longer penalized for no-signal sites** (`Site_Auditor_App.html` + `Site_Installer_App.html`, added 2026-07-30): previously, `adv()` (the shared handler behind every status button — Pre-visit/Pre-install call, On the way, At Site, Reschedule) captured the action's timestamp client-side (`new Date().toISOString()`, before any network call — already correct), but if the subsequent `sbPatch`/`sbGet` failed, the code reverted `o.status`/`j.status` and discarded the log entry entirely, forcing the worker to redo the action later at a *new*, later timestamp — unfairly affecting Arrival On Time % (notes 58/74/79/80) for a worker who genuinely arrived on time but had no signal. The installer app's final completion write (`reallyDone`) had the identical flaw with zero retry mechanism at all (a known, previously-documented gap — see note 72).
    - **Auditor app**: one localStorage slot per order, `md_audit_actq_<pi>` → `{id,status,log}`, overwritten wholesale on every failed `adv()` call (safe since `o.log`/`o.status` already accumulate monotonically in memory across repeated taps — each queued write is a strict superset of the last). On failure, `o.status` is now kept (never reverted) and the write is queued instead of discarded; toast changes from "Network error — try again" to "Saved on your phone — will sync automatically once you're back online".
    - **Installer app**: `adv()` is a read-modify-write against a `subjobs` array shared with other installers/subjobs on the same order, so a queue entry can never be blind-PATCHed from a locally-cached snapshot (would risk clobbering a sibling's concurrent edit). Instead, one slot per subjob, `md_install_actq_<pi>_<sjId>` → `{id,sjId,status,logEntries:[...]}` (append-only, each entry keeping its own originally-captured timestamp) — at flush time, always re-`sbGet` fresh, locate the subjob, apply every queued entry, then `sbPatch`, identical to `adv()`'s existing online logic just deferred until connectivity is confirmed.
    - **Flush mechanism**: `_flushActionQueue()` in both files (mirrors the existing `_retryingCompletion`-style reentrancy guard), triggered from the same place the auditor app's pre-existing `_retryPendingCompletions()` already fires (the first line of `loadJobs()`), plus a new `window.addEventListener('online',...)` in both files for faster recovery the instant connectivity returns (the 30s poll remains the safety net — `online` isn't a fully reliable signal that Supabase itself is reachable).
    - **Poll-clobber guard**: `loadJobs()` in both apps fully rebuilds its order/job array from the server every 30s. Added a small `ACT_ORDER` rank table (`callpending:1,onway:2,atsite:3,completed:4,reschedule:4`) so, in the same `existing[...]`-carryover block already used for `jobcard`, a freshly-polled row's status is compared against any pending queue entry for that order/subjob: if the queue is still "ahead" of the server, the local status/log is kept on screen instead of being clobbered back to the stale server value; if the server has caught up, the now-stale queue entry is garbage-collected from localStorage.
    - **Installer completion queue** (closes note 72's gap): `reallyDone` split into its existing two sequential writes (small `{status,log}` via `sbPatch`, then larger `{subjobs}` w/ jobcard via `sbPatchLong`), each now independently queued/retried on failure — mirrors the auditor app's existing `md_audit_ps_<pi>`/`md_audit_pjc_<pi>` pattern exactly, as `md_install_ps_<pi>_<sjId>` (`{id,sjId,log}`, target status is always `'completed'` by construction) / `md_install_pjc_<pi>_<sjId>` (`{id,sjId,jobcard}`, flush re-fetches fresh subjobs and splices the jobcard in — never a blind overwrite). New `_retryPendingInstallCompletions()` mirrors `_retryPendingCompletions()`, wired into the same `loadJobs()`/`online` trigger points. `_archiveInstallJobcard` still fires exactly once, only after a successful (possibly retried) write.
    - **`_uploadPhoto` hardening** (both files, byte-identical helper): added a 20s `AbortController` timeout — previously had none at all, so a hung upload (e.g. taking the arrival photo with no signal) could stall the arrival-confirm tap indefinitely before ever reaching its existing base64 fallback. All 3 existing call sites already wrap this in try/catch with that fallback — zero call-site changes needed.
    - **Pending-sync banner** (both files): new `#pendingSyncBanner` (adapts `#locDeniedBanner`'s fixed-position + body-class + push-down mechanics, `z-index:59` — one below `locDeniedBanner`'s 60 since a location-permission problem is more actionable) shows "⏳ N update(s) saved — syncing automatically" whenever any `actq`/`ps`/`pjc` key exists for that app; updated after every `loadJobs()`/flush call.
    - **Verified** (no test suite/staging exists for this app — see the safe-testing method in [[feedback_material_depot_ui_and_testing]]): served locally, faked `md_user` sessions, and used Playwright route interception (block/fulfill specific Supabase URLs) rather than any real writes. Confirmed for both apps: repeated offline taps accumulate correctly with distinct preserved timestamps and no status reversion; a successful flush clears the queue; the poll-clobber guard keeps a locally-ahead status on screen against a stale server response and garbage-collects the entry once the server catches up; the installer completion queue survives a failed retry and clears + archives correctly on a successful one; the upload timeout aborts at ~20s (measured) instead of hanging; the banner shows/hides and counts correctly in both apps.
    - Do not let the installer completion-queue flush skip its own fresh `sbGet` even though it "seems redundant" with the queued data — that fresh fetch is the load-bearing property that keeps it safe against a sibling installer's concurrent edit, mirroring why `_archiveInstallJobcard` (note 72) does its own independent fetch too.

85. **Three-part SM/training change: frozen sidebar + mandatory logged notes + Site Shadower training role** (2026-07-31, committed `3a2c8cb` on `master`, pushed to `origin/master`, deployed via `vercel --prod` — all 7 changed HTML files byte-diff identical to local on `https://material-depot-site.vercel.app`, both new files return 200; user confirmed they ran the prerequisite SQL). One approved plan, three independent workstreams. Verified with Playwright against a locally-served copy using faked `md_user` sessions and route-aborted Supabase (no real writes) — the established safe-testing method (53/53 checks passed).
    - **(A) Frozen left sidebar** (`SM_Audit_Dashboard.html` + `SM_Install_Dashboard.html`): the `.rail` nav was a static in-flow flex child that scrolled away with the page. Added `position:sticky;top:56px;align-self:flex-start;height:calc(100vh - 56px);overflow-y:auto;` to the existing `.rail` rule (audit width 208px, install 212px). `align-self:flex-start` is load-bearing — without it the flex default `stretch` gives the rail full height and sticky has no travel. `top:56px` clears the sticky header (which keeps `z-index:40` > the rail's auto, so the header still paints over it). The `@media(max-width:900px){.rail{display:none}}` breakpoint is untouched. `#connBanner` (sticky `top:56px`, outage-only) will paint over the rail's top strip while shown — cosmetic, left as-is.
    - **(B) Mandatory, strictly-required, logged notes on every MANUAL SM action** (both SM dashboards): a new shared `requireNote(actionLabel, preface)` helper (next to `toast`/`fmtLog` in each file) does a native `prompt()` and returns the trimmed non-empty note, or `null` on cancel/blank — **callers MUST `return` and write nothing on `null`**. Note text is embedded into the log entry's `t` string so it surfaces in the drawer Activity timeline with no render change (log shape unchanged, note 39). Wired into: (a) both `[data-setstatus]` handlers — the old conditional `overrideReason` prompt was replaced by a `forcedCompletedNoCard` flag; the ONE `requireNote` call now covers every manual status set (the force-Completed-without-card path reuses the note as the note-55 override reason, so there is never a double prompt; the At-Site manual override now ALSO prompts — this reverses note 76's "never prompt" stance, done deliberately per the current explicit instruction, see note 76 update); (b) manual `partial` (install — falls through the cascade and gets a note); (c) the audit-only back-stepper (`.step[data-goto]`); (d) follow-up `#setFollowUp`/`#clearFollowUp` in both files (these were previously **silent — no log entry at all**; now they require a note AND write a log entry with `log:o.log` added to the patch); (e) audit `#saveFollowUp` now requires its `#reschedRemark` textarea be non-empty. **Install ordering subtlety**: in the install `[data-setstatus]` handler the `requireNote` call is placed BEFORE the `created`-revert cascade (which mutates `o.subjobs` in memory and archives job cards) so a cancelled note can never leave the in-memory order half-reset. AUTO transitions are never gated — `requireNote` lives only in the SM click handlers, never in `syncParent` (install auto-`partial`) — so auto rollups stay silent by construction.
    - **(C) Site Shadower role + date-bounded training lifecycle**. Model: a SM trainee keeps `role='service_mgr'` (permanent primary) plus dated secondary stages in the new `profiles.training_stages` jsonb array (`{role,start,end}`, `role ∈ {site_shadower,site_auditor}`). `site_shadower` is never a primary `role` (so `profiles_role_check` is not extended). Shared helper `activeSecondaryRole(stages)` (duplicated in Login/both SM dashboards/Site_Auditor_App/Site_Shadower_App) returns the secondary role covering today via the same lexicographic `YYYY-MM-DD` compare as `active_from`; `null` between/after stages → pure SM. 
      - **Session**: `Login.html successScreen()` now persists `training_stages` into `md_user`. Both SM dashboards render a header "⇄" toggle (`#roleSwitch`) to the active secondary app when `activeSecondaryRole` is truthy, and do a best-effort background `sbGet('profiles?...&select=training_stages')` refresh (sessions never expire, so a schedule set after login isn't in `md_user` yet) then re-render the toggle.
      - **Guards**: `Site_Auditor_App.html` admits real `site_auditor`/`auditor_installer` OR a `service_mgr` with an active `site_auditor` stage; new `Site_Shadower_App.html` admits `site_shadower` (future-proof) OR a `service_mgr` with an active `site_shadower` stage. A plain SM (`training_stages=[]`→null) is rejected. NOTE for testing: a rejected `service_mgr` is redirected to `Login.html`, which auto-forwards a logged-in SM to `SM_Audit_Dashboard.html` — so a "bounce" lands on the SM dashboard, not Login. Field apps show a "⇄ Service Manager" return toggle for a `service_mgr` session (`#switchSM` in the auditor app; the shadower app's own).
      - **`Site_Shadower_App.html`** (new, purpose-built ~schedule-only clone of the auditor visual system, NOT a strip of the 106KB auditor file — deliberately has zero job-card/scanner/PDF/autosave machinery): queries `audit_orders?shadower_email=eq.<ME.email>`, renders the same day-strip + read-only cards (customer/PI/BM/phone/address/time/status + a "👁 Shadowing: <auditor>" chip), 30s poll + visibility refresh, own `manifest_shadower.json` + `pwa-install.js` + `sw.js`. The job card is DEFERRED to a later spec — do not add one here without that spec.
      - **Admin editor** (`Admin.html`): `openEditRole` stashes `window._editUser` and shows a "🎓 Training schedule" button ONLY for `service_mgr` rows → `openTrainingSchedule()` modal with add/remove `{role select (Shadower/Auditor), start, end}` rows, saved by `submitTrainingSchedule()` via `sbPatch('profiles',id,{training_stages})` (try/catch surfaces a missing-column error). `submitEditRole`/`openAddUser` untouched (role select does NOT offer site_shadower — it's not a primary role). `site_shadower` added to `ROLES`/`ROLE_ICONS`/`ROLE_DESCS` for display only.
      - **Shadow assignment**: `shadower_email`/`shadower_name` threaded through `AUDIT_COLS` + `mapAuditRow` (`o.shadowerEmail`/`o.shadowerName`). A new `loadShadowers()` (`role=eq.service_mgr` filtered client-side to those with an active `site_shadower` stage today) populates a SEPARATE `SHADOWERS` list — deliberately kept out of `AUDITORS`/`loadAuditors` so it never touches the auditor picker/cap/2-hour-conflict logic. The SM Audit "Assign auditor" drawer section shows an optional "Shadowed by" `<select id="shadowerSelect">` (rendered only when a trainee is assignable or one is already set); the `#assignAuditor` handler includes `shadower_email`/`shadower_name` in the same PATCH and logs the assignment. Nulled on both backward-move paths (setstatus handler + back-stepper) alongside the auditor fields.
      - **v1 scope / deferred** (confirmed with user): shadower job card = deferred (next spec). Auditor-stage trainees are observe-only in v1 — they are NOT added to `loadAuditors`, so an SM trainee in a `site_auditor` stage gets the auditor dashboard via toggle but won't be assigned real audits (would ripple into cap logic + Store Team counts, notes 44/50 — a follow-on). Install-side shadowing (`subjobs[].assignments[].shadower_email`) = deferred; v1 shadowing is audit-side only.
    - **Prerequisite SQL** (in "Pending SQL" above): `training_stages` on `profiles`, `shadower_email`/`shadower_name` on `audit_orders`. The training editor save and shadower assignment silently no-op until these columns exist.

86. **Scheduled-but-unassigned installs now appear in the SM Install schedule tab** (`SM_Install_Dashboard.html`, added 2026-07-31, committed `48ce586`, pushed to `origin/master`, `vercel --prod`, byte-diff confirmed live): user reported an install order that's scheduled (slot booked) but has no site installer assigned yet did NOT show in the schedule tab, even though the equivalent already works in SM Audit — their workflow is to book the installation time first, then assign installers a day before per availability. Root cause: the three install schedule surfaces (`scheduleView()` "Today's installs", `sjsForDay(ds)` powering the 📅 Schedule calendar day-columns + selected-day detail, and `installOrderHasDate(o,ds)` for the Orders-view date filter) all derived their day-lists purely from `sj.assignments[]` with only a legacy `sj.installer` fallback. But the 2-step booking flow's Step 1 ("Book slot", `#bookSlot_<sjId>` handler) sets `sj.date`/`sj.slot`/`sj.status='scheduled'` with **no** assignment — the installer is chosen later in Step 2 — so a scheduled-unassigned subjob had an empty effective-assignments list and silently vanished from the schedule until an installer was attached. SM Audit never had this bug because it keys its schedule off `o.date`, which is set the moment a slot is booked regardless of auditor (`scheduleView`/`calendarView` filter on `o.date===ds`, showing a red "Unassigned" for `status==='scheduled'`).
    - **Fix**: new shared helper `sjEffectiveAssignments(sj)` (defined just above `installOrderHasDate`) returns, in order: real `sj.assignments` if any; else the legacy `sj.installer` synthetic entry; else — **only when `sj.status==='scheduled' && sj.date`** — a synthetic `[{installer_id:null, installer_name:null, date:sj.date, dates:[], mode:'standard', slots:sj.slot?[sj.slot]:[], primary:true}]`; else `[]`. Routed `scheduleView()`, `sjsForDay()`, the calendar's `primaryName` + `detCards` name/asgns computation, and `installOrderHasDate()` through it. `installer_name:null` renders the existing red "Unassigned" markup in every surface (the row/name templates already did `a.installer_name||'Unassigned'`).
    - **Gated on `status==='scheduled'`, not merely `sj.date` present** — deliberately, so a `reschedule` subjob still carrying a stale `sj.date` (assignments cleared, awaiting rebook) does NOT reappear in the calendar; it stays only in the "To reschedule" tab. Once rebooked, Step 1 sets status back to `scheduled` and it correctly reappears.
    - **Deliberately left untouched**: `flLoad`/`wpSlotLoad` (installer *workload*/capacity counters — an unassigned subjob adds zero installer load, so it must NOT be counted there; a synthetic `installer_id:null` wouldn't match anyway) and `subjobSummary` (Orders-table subjob chips already showed `fmtDate(sj.date)` + "Unassigned"). Field `Site_Installer_App.html` is NOT affected — it only shows a worker their own assigned jobs, and an unassigned order has no installer to show it to.
    - **Verification**: Playwright/Chromium was not installed in the environment, so instead of a browser run the three membership functions (`sjEffectiveAssignments`/`sjsForDay`/`installOrderHasDate`) were extracted verbatim from the file (brace-matched by name) and executed in Node against six synthetic orders — 10/10 assertions passed: scheduled-unassigned now appears on its booked day; assigned + legacy-installer-field subjobs still appear; delivery-phase (no date), created-without-slot, and scheduled-for-a-different-day cases stay correctly hidden; the synthetic entry carries the right date/slot with a null name. All `<script>` blocks still parse. Live byte-diff of `SM_Install_Dashboard.html` against `https://material-depot-site.vercel.app` post-deploy was identical (matching SHA-256).
    - **How to apply**: if a future "X doesn't show in the install schedule" report comes in, first check whether the surface keys off `sj.assignments[]` vs `sj.date`/`sj.status` — the 2-step flow means a subjob's date exists before any assignment does. Do not remove the `status==='scheduled'` gate on the synthetic branch (it's what keeps stale-date reschedule subjobs out), and do not route the workload counters (`flLoad`/`wpSlotLoad`) through `sjEffectiveAssignments` (an unassigned slot is not installer load).

87. **Cross-visibility of booked audit slots between the SM Audit dashboard and the Store Team app — closed a real double-booking gap** (`Store_Team_App.html` + `SM_Audit_Dashboard.html`, added 2026-08-04; PHASE 1 of a 4-part plan at `~/.claude/plans/humble-sleeping-boole.md`): both apps read/write the same `audit_orders` table but represented a slot differently — Store Team knows only its 6 fixed hourly IDs (`10:00/11:00/13:00/14:00/16:00/17:00`), while the SM books via `<input type=time>` so `o.slot` is an arbitrary `"HH:MM"`. Two independent gaps let each side double-book the other:
    - **Store side dropped every off-grid SM booking.** `getAvailability(slotId)` matched an order's slot with `SLOT_DEFS.find(s=>s.id===o.slot)`, so any SM audit at a non-fixed time (`10:30`, `12:00`, `15:00`, …) returned `undefined` and was skipped entirely — it did NOT reduce the store's shown availability, and appeared in the "All confirmed bookings" list with a `—` slot tag. **Fix (Store_Team_App.html)**: new `slotStartMin(slot)` resolves a start-minute for any format (fixed id → its `startMin`; `"HH:MM"` → `h*60+m`; legacy `sf/sw`/null → `null`, deliberately NOT half-resolved). `getAvailability` now builds a pseudo-slot `{startMin,endMin:startMin+60}` and reuses the existing `slotsConflict` 120-min rule instead of exact-ID matching. `fmtSlotId` gained an `"HH:MM"`→12h branch (stays a function, note 6/38). New per-slot red "engaged" badge (`.slot-badge.engaged`) shows ANY confirmed audit that conflicts with each fixed slot, so store staff see what's actually engaged (incl. off-grid times), not just a number.
    - **A3 — the `absorbed` dedup had to become minute-based or the above would double-count.** A store pre-booking (`slot_reserved`, no auditor) is skipped when its own real service order already exists for the same customer; that check used exact `r.slot===o.slot`, which fails once the real order is at an off-grid time. Changed to a `slotBlocksOverlap` (|start gap|<60) match keyed on the same PI/name linkage, so a `10:00` pre-booking + its real `10:30` order count once, not twice.
    - **SM side never warned about store pre-bookings.** `auditorConflictOrder` scanned only `BOOKED` (which excludes `slot_reserved`), so assigning an auditor over a store pre-booking that auditor was already reserved onto raised no warning. **Fix (SM_Audit_Dashboard.html)**: new `const CONFLICT_STATUSES=[...BOOKED,"slot_reserved"]` used ONLY inside `auditorConflictOrder` (do not mutate `BOOKED` — it's shared by `slotUsage`; do not touch `auditorLoad`/`capFor` cap math — a no-auditor reservation belongs to no auditor's cap). A `slot_reserved` with no `auditor_id` never matches the `o.auditor===aid` predicate, so no-auditor reservations correctly warn nobody. Conflicts stay on the soft, overridable `.conflict`/`confirm()` path (note 52) — never the `full`/click-block path.
    - **B3 — prevented a self-warn.** With `slot_reserved` now in the conflict set, the normal assign picker `_renderAudList` (which, unlike `slotResAudList`, had no linked-order exclusion) would warn about the SAME customer's own pre-booking when assigning them the reserved auditor. Mirrored `slotResAudList`'s `linkedSvc` logic in reverse: compute `linkedPre` (the `slot_reserved` row matching this real order's slot/date + `po[0]===o.pi` or name) and pass `linkedPre?.id` as `auditorConflictOrder`'s `excludeId2`.
    - **No schema change.** Verified via the Node-extraction method (Playwright not reliably available — same approach as note 86): the edited functions were brace-extracted and run against synthetic data — 26/26 assertions (the exact worked examples: `12:00` blocks the 10/11/13/14 fixed slots, `10:30` blocks 10/11/13, `15:00` blocks 13/14/16/17; legacy/`slot_converted`/`deleted` change nothing; A3 dedup; SM slot_reserved-with-auditor conflicts, no-auditor warns nobody, `excludeId2` suppresses the self-warn, genuine order still conflicts) plus `fmtSlotId` 8/8, and both files' inline `<script>` blocks parse.
    - **How to apply**: the two apps still do NOT share a slot vocabulary or a stored per-slot lock — cross-visibility works only because both read the same `audit_orders` and now both compare real minute-times. If a future double-booking report appears, check first whether a NEW surface reintroduced exact-ID slot matching (`s.id===o.slot`) instead of `slotStartMin`, or excluded `slot_reserved` from a conflict set again. PHASES 2-4 of the same plan (Hyderabad/city segregation, resumable partial job cards, per-SKU install splitting) are the natural pickups.

88. **City segregation (Bengaluru/Hyderabad) — new `city` dimension across orders + staff, deploy-safe via a probe-gate** (`Store_Team_App.html` + `SM_Audit_Dashboard.html` + `SM_Install_Dashboard.html` + `Admin.html` + `docs/supabase_slim_views.sql`, added 2026-08-04; PHASE 2 of the 4-part plan at `~/.claude/plans/humble-sleeping-boole.md`): Hyderabad stores (Gachibowli, Kompally) join the existing Bengaluru centres; HQ's one SM team schedules for both cities via a Bengaluru⇄Hyderabad toggle, and audits/installs/auditors/installers are each tagged with a single `city`.
    - **Schema (new)**: `city text` on `profiles`, `audit_orders`, `install_orders` (SQL in "Pending SQL" above; existing rows backfilled to `'Bengaluru'`); `install_orders_slim` re-created to SELECT `city`. Decisions locked with the user: **one city per staff member** (single `profiles.city`, not a multi-city table); per-SKU splitting/partial cards are separate later phases.
    - **THE deploy-safety mechanism — probe-gate (`CITY_ENABLED`)**: I (assistant) can't run Supabase DDL, and adding `,city` to an explicit-column `select=` returns HTTP 400 (`sbGet` passes the non-array error object straight through → the SM poll trips `_setConnErr` and shows an empty board = outage) while a `city` write payload throws (`sbPost`/`sbPatch` `if(!r.ok)throw` → order/user creation breaks) if the column is absent. So each file, at boot, `await`s a per-RELATION probe (`sbGet('<rel>?select=city&limit=1')`) and sets `CITY_ENABLED = every probe Array.isArray`. Probed relations per file: Store Team + SM Audit → `profiles` AND `audit_orders`; SM Install → `profiles` AND `install_orders_slim` (**the VIEW** — this is what distinguishes "base column added but view not recreated" from "fully migrated", the one partial-migration state a base-table probe would get wrong); Admin → `profiles`. When false, EVERY city touch is skipped: explicit selects omit `,city`; write payloads spread `...(CITY_ENABLED?{city}:{})`; the header toggle + Add-Order/Add-User City fields stay hidden; `cityScope`/`auditorsInCity`/`installersInCity` are the identity. So pre-migration the app is byte-behaviorally identical to before, and it **self-activates with no second deploy** the instant the SQL + view re-run land. Verified live: all three probes currently return 400 on prod (columns don't exist yet) → `CITY_ENABLED=false` everywhere.
    - **Store→city map** `STORE_CITY` (single source of truth) lives ONLY in `Store_Team_App.html` beside the extended `STORES` (now 7: +Basaveshwar Nagar, +Gachibowli, +Kompally — supersedes note 83's 4). SM dashboards NEVER key city off a store name or the free-text `bm` (note 68) — they read the order's stored `o.city` and stamp new orders from the toggle. Store Team derives city from the selected store, writes it into the pre-booking, and **city-scopes the `AUDITOR_COUNT`** in `loadDay` (the city clause is ANDed with — never replaces — the existing `active_from` filter, note 44).
    - **City toggle** = header segmented control `{All, Bengaluru, Hyderabad}`, module `let cityFilter` persisted to `localStorage.md_city`, default last-used (seeded `'all'`). It is a **cross-view context, deliberately NOT added to the nav-reset lines** (which only clear `filterStatus`/`filterDate`) since it also scopes the Schedule/Need-Action/pickers, not just the Orders list. `cityScope(list)` is applied to BOTH the stat-tile counts AND the row list in each `ordersView` (note 82 sum-to-total: install's 6 buckets still sum to the city total), plus every user-facing view (schedule/today/reschedule/followups/calls/rectifications/nav counts). Installer **workload** counters `flLoad`/`wpSlotLoad` are deliberately left unscoped (an installer is single-city, so their load is already within-city — note 86's caution).
    - **Order creation**: an explicit City `<select>` (shown only when enabled) in each Add-Order modal, defaulting to the active toggle's city (or Bengaluru when toggle='all', since 'all' is not a stampable city). Kylas import reuses the same `submitAddOrder`, inheriting that field — no unreliable address parsing. Rectification orders copy the source order's `o.city`. Staff city is set in Admin only (Add User + Edit Role) — SM Auditors/Installers views show a read-only city chip.
    - **Verification** (no test suite/staging; Node-extraction per notes 86/87): 24/24 assertions across BOTH states — `STORE_CITY` map, `cityScope`/`auditorsInCity`/`installersInCity` inert-vs-active, select-builders omit `,city` when inert, `mapAuditRow`/`mapInstallRow` default null city to `'Bengaluru'`, and Store Team's auditor count ANDing city with `active_from` — plus a live prod probe proving all three relations 400 today (inert). All 4 files' inline `<script>` blocks parse; ASCII-only (note 15).
    - **How to apply**: if a future report says "Hyderabad orders/auditors aren't showing" or "the toggle isn't appearing," check IN ORDER: (1) did the user run the 3 `ALTER`s AND re-run `docs/supabase_slim_views.sql`? (the view re-run is the easy-to-miss step — `install_orders_slim?select=city` 400ing keeps SM Install inert even after the base ALTER); (2) is `CITY_ENABLED` true in that page's console; (3) do the relevant `profiles.city` values exist (staff created before the migration default to Bengaluru until edited). Do NOT reintroduce a `bm→city` inference, and do NOT route `flLoad`/`wpSlotLoad` through `cityScope`. PHASE 4 (per-SKU install splitting) is the remaining pickup.

89. **Resumable partial job cards for installations** (`Site_Installer_App.html` + `SM_Install_Dashboard.html` + `Admin.html`, added 2026-08-04; PHASE 3 of the plan at `~/.claude/plans/humble-sleeping-boole.md`): an installer can mark an install sub-job **partially completed** (records the rooms/work done so far + a MANDATORY note + OPTIONAL signature), the sub-job enters a resumable `partial` state, and a partial job-card PDF is available; later the same or a different installer reopens it, the prior rooms load, they add the rest and complete normally (full T&C→ratings→signature). **No schema change** — rides the `subjobs` jsonb. User-confirmed lightweight (no formal planned-vs-remaining checklist).
    - **`partial` is a first-class SUB-JOB status now** (previously only an order-level rollup). New artifact shape distinct from draft (`{draft:true,rooms}`) and completed (`{rooms,sign:{...ratings}}`): `sj.jobcard={partial:true, rooms:[...with photos], note, sign?:{img,name}, partialBy, partialAt}`, `sj.status='partial'`.
    - **THE critical ordering (do not reorder)**: in `openJobCard`, the partial-resume branch runs BEFORE the note-64 signed-lock (`sj.jobcard.sign&&!sj.jobcard.draft`). A partial may carry an OPTIONAL signature, so without checking `partial` first it would be mistaken for a terminal signed card and lock edits. On resume, `jcResumePartial=true`; `autosave`'s guard is `if(jcHadSign||jcResumePartial){local-only}` — so a resume never DB-writes via the draft path (mirrors note 64), only via the explicit re-mark-partial or completion writes.
    - **`markPartialComplete`→`_writePartial` mirrors `reallyDone` exactly**: early-cancel autosave (`_autosaveSeq++`), two writes (status+log via `sbPatch`, then `{subjobs}` with the jobcard via `sbPatchLong`), and **sets `_completionWrite` before the long write** so an in-flight draft autosave re-issues instead of clobbering (the two-layer race fix, notes 2/64 — untouched). Its OWN retry queues `md_install_ppart_ps_`/`md_install_ppart_pjc_` (NEVER reuses `ps_`/`pjc_`, which hardcode `sj.status='completed'` on flush) + `_retryPendingInstallPartials()` (sets `partial`, never completed), wired into `loadJobs` + the `online` listener + the pending-sync banner. `ACT_ORDER` gained `partial:4` (atsite<partial<completed).
    - **`rollupStatus` (installer) + `syncParent` (SM)** each got an explicit `some(s==='partial')→'partial'` rung so a lone partial sub-job rolls the ORDER to `partial` deterministically (added after the existing "some completed && some not" rung; still behind the delivery-phase-divergence check, so a mixed order with a delivery-phase sibling stays `mixed`).
    - **`_archiveInstallJobcard` guard widened, dedupe preserved** (both installer + SM copies, kept verbatim): archives when `sign||partial` (was `sign` only, note 72); dedupe key is `sign.img ?? partialAt` — **the `sign.img` path stays intact for signed cards (note 72)**, unsigned partials use their own per-visit `partialAt` so each real visit archives exactly one row. Per-visit attribution (installer A visit-1, installer B visit-2) rides `jobcard_history` + each visit's own `atsite` arrival log — note-79 arrival reconstruction is untouched (partial writes are `by:"auto"` and never rewrite arrival entries).
    - **Admin analytics**: sub-job `partial` was previously UNREACHABLE (no sub-job was ever `partial`), so dropping it from the done/completed sets changes ZERO historical numbers while correctly treating future partials as in-progress: removed from `iDone` (Completion % numerator), `_anInstallCanon`'s `isDone` (so a partial row is dated by scheduled date, not a nonexistent completion log), the rating-attach `doneRows`, the per-installer `completed` count, the completion drill, and today's-schedule `done` flag — but KEPT in `iArrivalEligible` (`['completed','partial','atsite']`) so a partial visit still counts for Arrival %. `JOB_STATUS`/`.c-partial` (Admin), installer `STATUS`/`.b-partial`, and SM `STATUS.partial`/`sjChip` all render the chip; SM note-82 stat buckets already fold `partial` into "Scheduled/in progress" so they still sum to total. Admin `openJobDetail` shows a teal "Partially completed — resumable" banner (before the `isDraft` amber banner).
    - **PDF**: all generators already tolerate a partial (rooms present, sign/ratings gated as optional). Installer `genPDF` polished to title "(Partial)", "Rooms installed so far", and print the note.
    - **Verified** (Node-extraction — Playwright/Puppeteer not installed, per notes 84-87): 22/22 assertions — `rollupStatus`/`syncParent` partial rungs (incl. mixed-order divergence still `mixed`), `ACT_ORDER` ordering, `_archiveInstallJobcard` guard+dedupe (partial archives, `partialAt` dedupe, `sign.img` dedupe preserved, neither-skipped), and the analytics in-progress-vs-arrival-eligible split. All 3 files' `<script>` blocks parse; no smart quotes in the new code. **NOT browser-E2E'd** (no automation env) — the write path is a structural mirror of the already-proven `reallyDone`. **How to apply**: never reorder the partial-check-before-signed-lock in `openJobCard`; never reuse `ps_`/`pjc_` for partial; keep `partial` OUT of `iDone` but IN `iArrivalEligible`.

## Pending POs Import (replaces Kylas Sheet as of 2026-06-22)

**Constant**: `POS_API='/api/pos'` in both SM dashboards (Vercel rewrite proxies to `https://api-dev2.materialdepot.in/apiV1/site-audit-installation-pos/` — avoids CORS)  
**SM Audit** fetches `?type=site_audit&page_size=100`; **SM Install** fetches `?type=installation&page_size=100`  
**Search**: `?search=<term>` — server-side filter by PO number, customer contact, or lead ID. Debounced 400ms via `debouncePOSearch()`.

### API → Form field mapping
| API field | Form field | Notes |
|---|---|---|
| `estimate_lead_id` | `ao-pi` | ENQ... format — fully auto-filled now |
| `po_number` | `ao-po` | |
| `customer.name` | `ao-name` | |
| `customer.contact` | `ao-phone` | integer → String |
| `bm.name` | `ao-bm` | |
| `shipping_address.address` | `ao-addr` | fully auto-filled now |
| `delivery_date` | `ao-delivery` | install only |
| `skus[].variant_handle` | `ao-sku-code` rows | install only; type from product_name contains 'wallpaper' |

**Deduplication**: `existingPOs = new Set(ORDERS.flatMap(o => o.po || []))` — checks `r.po_number`. Imported rows shown collapsed under "already imported" `<details>`.  
**Card display**: customer name+phone, PI+PO+BM+date+po_status chip, address snippet + 📍 map link, SKU product names (first 2).  
**po_status colours**: `dispatch_pending`/`pickup_attempted` → amber; `delivered` → green; `cancelled` → red.

## Deployment Workflow
```bash
git add <specific files>
git commit -m "description"
git push origin master
vercel --prod
```
Always add specific files, not `git add .`.
